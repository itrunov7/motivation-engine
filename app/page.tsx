export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 p-8">
      <h1 className="text-2xl font-semibold tracking-tight">
        Motivation Engine — Control Center
      </h1>
      <p className="max-w-md text-center text-sm text-[#8CA495]">
        Baseline skeleton. The shelves (registry, sources, dossiers, decisions,
        docs) exist as folders in the repo; the showcase pages arrive in the
        next build steps.
      </p>
    </main>
  );
}
