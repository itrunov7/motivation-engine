{
  "decisions": [
    {
      "id": "D-001",
      "date": "2026-07-12",
      "title": "JSON files in git are the single source of truth",
      "body": "All project data lives exclusively in versioned files in the repo: /registry/**.json, /sources/sources.json, /decisions/decisions.json, /dossiers/**, /docs/**.md. No databases, no data inside components, no external stores at baseline. Versioning, review, and status history come from git itself. Escalation away from files happens only when the triggers in docs/architecture.md fire (corpora arrive, >500 implementations, etc.).",
      "area": "architecture"
    },
    {
      "id": "D-002",
      "date": "2026-07-12",
      "title": "/cards are generated projections, never hand-edited",
      "body": "Human-readable mechanism cards in /cards/{id}.md are output only, rendered from registry JSON by tools/render-cards.ts (npm run cards). They are never created or edited by hand; any change to a card goes through its source record in /registry/mechanisms and a regeneration. This keeps exactly one editable representation of each mechanism.",
      "area": "process"
    },
    {
      "id": "D-003",
      "date": "2026-07-12",
      "title": "A mechanism without metrics or hard constraints is invalid",
      "body": "A full L1 record must have non-empty implementations[].metrics and non-empty constraints.hard_rules, otherwise the validator rejects it. Rationale: a mechanism we cannot measure is not knowledge, and a mechanism without guardrails is a dark-pattern risk. The schema is not weakened for convenience; seed stubs use a separate reduced sub-schema in /registry/mechanisms/_seed/ instead.",
      "area": "data"
    },
    {
      "id": "D-004",
      "date": "2026-07-12",
      "title": "Core roster is seed v0.9 — candidates until dossiers pass",
      "body": "The 12-mechanism roster (LA-01 plus 11 stubs) is a seed hypothesis, not a settled core. All stubs carry lifecycle_status 'candidate' and contain only id, name, grade_draft, oneliner, parent. Promotion to incubating/core happens exclusively through scored dossiers (5 axes, thresholds per /dossiers/dossier.schema.json), entered by the owner or the future derivation pipeline — never by fleshing out stubs in code.",
      "area": "data"
    },
    {
      "id": "D-005",
      "date": "2026-07-12",
      "title": "Showcase honesty contract: every status computed from files on disk",
      "body": "Every status shown in the Control Center (live / in progress / planned, counts, coverage) is computed at render time from actual data files. Hardcoding a status or a number in JSX/TS is forbidden. Where no data exists yet, the UI shows a designed empty state naming the file or pipeline that will fill it and the phase. The showcase may never look more finished than the shelves actually are.",
      "area": "architecture"
    },
    {
      "id": "D-006",
      "date": "2026-07-13",
      "title": "Single-password auth via HMAC cookie and middleware",
      "body": "Auth per SPEC §5: middleware.ts guards all routes except /login and static assets. /login posts to a server action that compares the submitted password against env ACCESS_PASSWORD (constant-time) and, on success, sets an httpOnly cookie 'me_session' containing '<expiresAtMs>.<hmac>' signed with HMAC-SHA256(env SESSION_SECRET), maxAge 30 days, sameSite lax, secure in production. In-memory sliding-window rate limit: 5 attempts/min per IP (x-forwarded-for), resets on server restart — acceptable at baseline (single instance). No auth libraries; Web Crypto in middleware (Edge) and node:crypto timingSafeEqual in the action. No accounts, roles, OAuth, session DB, or analytics. IDs D-001–D-005 are reserved for the owner-specified baseline decisions (SPEC §3.5).",
      "area": "architecture"
    },
    {
      "id": "D-007",
      "date": "2026-07-13",
      "title": "Hosting on Vercel with git-connected auto-deploy from main",
      "body": "Deployed to Vercel (project motivation-engine, personal scope), production URL https://motivation-engine-pi.vercel.app. GitHub repo itrunov7/motivation-engine is connected, so every push to main auto-deploys to production and every later step is verified live. Env vars ACCESS_PASSWORD and SESSION_SECRET are set as sensitive values in Production and Preview environments; the production SESSION_SECRET is freshly generated and distinct from the local dev secret, and the production ACCESS_PASSWORD replaces the dev placeholder. No custom domain, no additional infrastructure — file-based baseline runs as-is on Vercel serverless.",
      "area": "operations"
    },
    {
      "id": "D-008",
      "date": "2026-07-13",
      "title": "Tooling stack: tsx + Ajv for validator and card generator",
      "body": "tools/validate.ts and tools/render-cards.ts run via tsx (npm run validate / npm run cards) — no build step for tooling. Validation uses Ajv 2020-12 (ajv + ajv-formats) against the JSON schemas in the repo; the hard rule (non-empty implementations[].metrics and constraints.hard_rules, D-003) is additionally enforced by an explicit check in validate.ts so it fails loudly even if the schema were ever weakened. taxonomy.json, sources.json, and decisions.json are validated against inline schemas mirroring SPEC §3.1/§3.4/§3.5; dossier.schema.json is compiled for integrity and any future dossier records are validated against it including total = sum of axis scores. CI (.github/workflows/validate.yml) runs install, validate, and build on every push, so invalid knowledge physically cannot enter the repo.",
      "area": "stack"
    },
    {
      "id": "D-009",
      "date": "2026-07-13",
      "title": "Status computation centralized in lib/status.ts",
      "body": "All showcase block statuses (live / in progress / planned) are computed in a single server-only module, lib/status.ts — the only file in the codebase where status literals may appear; components map presentation through STATUS_META keys, never through literals. Rules per SPEC §4: Registry is live when at least one full record passes the hard rules (non-empty implementations[].metrics and constraints.hard_rules, D-003); Schema & validator when the schema files and the CI workflow exist; Card generator when /cards holds a generated {id}.md for a valid full record. Folder-driven blocks (runtime, corpora, telemetry) treat README.md, dotfiles, .gitkeep, and empty subfolders as non-data, and a corpus subfolder counts only when named after a source id from sources.json and containing a manifest.json (per the /corpora contract) — so an empty or fake folder can never turn a block green. Typed data loaders live in lib/data.ts and are reused by later showcase pages.",
      "area": "architecture"
    },
    {
      "id": "D-010",
      "date": "2026-07-13",
      "title": "Docs render via react-markdown; /sources filters via URL searchParams",
      "body": "The remaining showcase pages stay Server Components end to end. /docs/[slug] renders the five foundation documents with react-markdown + remark-gfm (the first runtime dependencies beyond Next itself) — server-compatible, no dangerouslySetInnerHTML, GFM tables supported; slugs are allowlisted in lib/data.ts (DOC_SLUGS) and unknown slugs 404. /sources filters (class A–D, priority P0–P2, status) are plain links that toggle URL searchParams and the table is filtered server-side — zero client JS, shareable filter URLs. /dossiers reads /dossiers/*.json via loadDossiers() (excluding the schema), so its empty state is computed from the folder being empty, never asserted; source status presentation lives in SOURCE_STATUS_META in lib/status.ts, keeping status literals out of app/ (D-005, D-009).",
      "area": "stack"
    },
    {
      "id": "D-011",
      "date": "2026-07-13",
      "title": "Constitution amendment: controlled external-API whitelist for tools/ scripts (Phase 2)",
      "body": "Baseline forbade all external fetches (former rule 12 in .cursorrules). Phase 2 opens a controlled whitelist: scripts under tools/ MAY call exactly four public APIs — api.openalex.org, api.semanticscholar.org, PubMed E-utilities (eutils.ncbi.nlm.nih.gov), and web.archive.org (CDX API). Any other external endpoint requires a new decisions.json entry first. The Next.js app remains file-based with zero runtime external calls from app/ (including middleware and server actions); data reading stays fs-only. LLM API calls remain forbidden everywhere in the repo. General scrapers stay forbidden; source connectors exist only as tools/ scripts against the whitelisted APIs.",
      "area": "architecture"
    },
    {
      "id": "D-012",
      "date": "2026-07-13",
      "title": "Connector framework: one contract, one runner, one manifest shape, 40 MB corpus guardrail",
      "body": "All source connectors implement a single Connector interface (tools/connectors/types.ts) and are executed only through tools/run-connector.ts (npm run connector -- <id> [key=value ...]). Every run — success, partial, or failed — writes /corpora/{source_id}/manifest.json: { source_id, connector_version, last_run {timestamp, status, params, records_fetched, files_written, duration_s, error?}, run_history (last 20 runs, newest first), data_files [{path, records, bytes}] }, so corpus statuses in the showcase stay computed from disk. Shared helpers bake the guardrails in rather than trusting each connector: fetch is rate-limited with 3 attempts and exponential backoff, sends polite User-Agent/mailto headers, and hard-rejects any host outside the D-011 whitelist; JSON output is pretty-printed for reviewable git diffs. Guardrail: a corpus directory exceeding 40 MB fails the run with a message pointing at the Postgres escalation trigger in docs/architecture.md (corpora at thousands-of-rows scale move to managed Postgres / object storage; the repo is never silently bloated). Connectors with a \"_\"-prefixed source_id (e.g. the dummy smoke-test connector) are internal and ignored by the showcase. The validator checks every corpus manifest against this contract.",
      "area": "architecture"
    },
    {
      "id": "D-013",
      "date": "2026-07-13",
      "title": "Source connection modes and manifest-derived statuses; app-store-reviews source added",
      "body": "\"0/30 connected\" was a false metric — it conflated four different source natures under one connectivity claim. Every source in sources.json now carries connection_mode: api (automated connector), report (one-off ingested artifact), manual (licensed human curation, never machine-harvested), or deferred (P2, not planned this phase; optional mode_note records why). The stored status field is removed entirely: states are computed in lib/status.ts from /corpora/{source_id}/manifest.json — an api source is connected iff last_run.status = \"success\"; a report source is ingested iff additionally at least one data_files entry exists on disk; manual and deferred sources show their mode, never a fake connectivity status. Owner assignment: api = openalex, semantic-scholar, pubmed-europepmc, wayback-cdx, sec-edgar, google-patents, app-store-reviews (7); report = revenuecat-report, unbounce-benchmarks, product-analytics-benchmarks, goodui, forrt (5); manual = mobbin, screensdesign, pageflows, paywall-galleries, growth-design, built-for-mars, gdc, baymard, engineering-blogs (9); deferred = appstore-screenshot-history, psyarxiv-osf, metabus, psychopen-cama, cochrane, neurosynth, neurovault, sensor-tower, ftc-eu-enforcement, company-handbooks (10). New owner-provided source app-store-reviews (App Store / Google Play review texts as emotional labels, class C, P1, api mode, $0 via public APIs / RSS feeds) is a distinct source from appstore-screenshot-history, which is deferred pending a paid-subscription purchase decision. No ad-libraries source is added — that is a September buy-vs-build decision with its own record. sec-edgar and google-patents record api intent, but their endpoints are outside the D-011 whitelist; building those connectors requires a new decision first. /sources and Overview group by mode: api n/7 connected · report n/5 ingested · manual 9 curation · deferred 10.",
      "area": "data"
    },
    {
      "id": "D-014",
      "date": "2026-07-13",
      "title": "Connectors decoupled from sources: manifest source_ids[] declares what a corpus harvests",
      "body": "A connector is not a source. The first real connector (evidence) harvests two registry sources — openalex and semantic-scholar — into one merged corpus /corpora/evidence/{mechanism_id}.json, which the D-013 rule (corpus dir name = source id) cannot express without dishonest states: either a fake \"evidence\" source record in sources.json, or marking actively-harvested real sources deferred. The manifest contract gains one field: source_ids: string[] — the sources.json ids the corpus harvests (empty for \"_\"-prefixed internal corpora). Status computation in lib/status.ts changes to: an api source is connected iff ANY /corpora/{dir}/manifest.json lists it in source_ids with last_run.status = \"success\"; a report source is ingested iff additionally that corpus has a data_files entry on disk. Corpus dir names no longer need to match source ids; the validator instead checks that every source_ids entry exists in sources.json and that non-internal corpora harvest at least one source. This future-proofs the framework: the wayback connector will list source_ids: [\"wayback-cdx\"], and any future connector serving multiple sources (e.g. one benchmarks ingester covering several report sources) works without new registry entities.",
      "area": "architecture"
    },
    {
      "id": "D-015",
      "date": "2026-07-13",
      "title": "Evidence terms live in mechanism records; corpus must be able to disconfirm",
      "body": "Search terms for the evidence connector are per-mechanism DATA, not hardcoded CLI params. The mechanism schema (full records) gains an optional evidence_terms: string[]; the connector reads terms from the record, falling back to [name] for records without the field (an explicit terms= param still overrides for ad-hoc runs). Rationale: generic terms harvest only confirming mainstream literature; dissent, replication, and boundary-condition papers require targeted terms — a corpus that can only confirm is methodologically broken. LA-01's owner-provided terms therefore include 'loss aversion replication', 'loss aversion moderators', 'loss of loss aversion', and 'loss aversion small stakes' alongside the mainstream ones. Second change: deduplication now unions DOI AND normalized title (lowercase, punctuation and asterisks stripped) into transitive clusters and keeps the highest-citation variant of each paper, instead of first-seen-wins by DOI only. Connector bumped to v1.1.0 (output gained terms_source and terms).",
      "area": "data"
    },
    {
      "id": "D-016",
      "date": "2026-07-13",
      "title": "Internal connection mode; four owner-provided sources added (ventora-telemetry, failure-postmortems, email-flow-galleries, cultural-ux-variation)",
      "body": "The connection_mode vocabulary (D-013) gains a fifth value: internal — data produced by our own platform, not an external source. An internal source proves connectivity the same way an api source does: connected iff any /corpora/{dir}/manifest.json lists it in source_ids with last_run.status = \"success\"; no status is ever stored. Four owner-provided sources enter sources.json: ventora-telemetry (class C, P0, internal — Amplitude events from Ventora-built products carrying mechanism_tags, the only source that fills observed_effects and promotes mechanisms to CORE), failure-postmortems (class D, P2, manual — the anti-survivorship corpus of startup autopsies and failed-product teardowns), email-flow-galleries (class A, P2, manual — lifecycle email and push-sequence libraries covering artifact types screen libraries miss), and cultural-ux-variation (class B, P2, deferred — geo/cultural variation of interface surfaces, a placeholder keeping the gap visible). The access enum grows by the four exact access strings; the vocabulary stays closed. tools/connectors/wayback-domains.json gains a dead_products section (empty, owner-filled) so the anti-survivorship track has a slot in the interface-evolution corpus. Rationale: Ventora telemetry deferred to September — no engineering capacity; M3 live test moves to the team integration phase.",
      "area": "data"
    },
    {
      "id": "D-017",
      "date": "2026-07-13",
      "title": "Connector harvests breadth; owner pins the tail — 100% recall is a human+machine contract, not a query-tuning goal",
      "body": "The evidence connector cannot surface every paper that matters for a dossier: citation-sorted search and finite per-term caps systematically miss low-citation boundary-condition and dissent works even after targeted evidence_terms (D-015). Full records gain an optional pinned_evidence: [{title, doi, reason}] array — owner-authored by hand. The connector merges pinned entries into /corpora/evidence/{mechanism_id}.json with source_api: \"pinned\", fetching metadata from OpenAlex by DOI (falling back to the owner title/doi if the lookup fails). Dedup still unions DOI and normalized title; when a pin overlaps a harvested record the pin wins source_api so the owner's intent stays visible. Rationale: chasing 100% recall by query-tuning is a false goal; the honest contract is breadth from the connector plus a human-pinned tail. LA-01 pins two dossier-cited boundary papers (Ert & Erev 2013 small-stakes; Walasek & Stewart 2015 disappear-and-reverse).",
      "area": "data"
    },
    {
      "id": "D-018",
      "date": "2026-07-13",
      "title": "Semantic Scholar authentication and graceful keyless degradation in the evidence connector",
      "body": "The evidence connector reads an optional S2_API_KEY env var and sends it as the x-api-key header — authenticated Semantic Scholar clients get materially higher rate limits than the shared keyless pool, which 429s in bursts and made full-success runs a lottery. Keyless degradation: on the first HTTP 429 without a key the run flips into throttled mode instead of failing — per-term batch drops from 15 to 10 for the retry passes, retry cooldowns become exponential (30s, 60s, 120s), and the manifest run entry records warnings: { s2_throttled: true }. The manifest contract (D-012) gains the optional warnings field (object of booleans) for connector degradation flags; the validator accepts it. CI: the validate workflow passes S2_API_KEY from the repository secret of the same name (empty when unset — connectors degrade gracefully); local usage is documented in the tools/ runbook. No behavior change otherwise — we are already API-only; this is authentication, not architecture. Connector bumped to v1.3.0.",
      "area": "operations"
    },
    {
      "id": "D-019",
      "date": "2026-07-13",
      "title": "Corpus completeness is verified structurally — review-reference coverage and category checklist — not by knowing the answers in advance",
      "body": "Evidence connector v2 (2.0.0) adds two structural completeness checks that need no ground truth. (1) Snowballing: after the term harvest, review/meta-analysis records are identified from the OpenAlex type field plus title keywords (meta-analysis / systematic review); for the top 2 by citations, their referenced_works are fetched from OpenAlex, coverage = the share of each review's resolved references already in the corpus, and references missing from the corpus that appear in ≥2 reviews' lists are auto-added with source_api \"snowball\". The coverage_report (per-review references_total / references_resolved / references_in_corpus / coverage, plus snowball_added) is written into the evidence file. (2) Category checklist: every record is classified into the non-exclusive categories foundational (≥1000 citations), meta-analysis (type/title), replication (title/abstract markers), dissent (documented disconfirmation markers in title/abstract/pin_reason), and recent (≤5 years old) — metadata only, the connector never judges scientific content. Records gain openalex_id, openalex_type, and categories; per-category counts go into the evidence file (category_counts) and the corpus manifest (data_files[].categories). The new /connectors cockpit renders the checklist per data file and flags any empty category red; red (#F87171) enters lib/status.ts as the one deliberate alert-token addition to the palette, for structural gaps and failed runs. Validator rule: a dossier referencing a mechanism whose evidence file is missing, unclassified, or has an empty dissent category FAILS — a corpus that can only confirm is broken.",
      "area": "data"
    },
    {
      "id": "D-020",
      "date": "2026-07-13",
      "title": "Corpus manifest contract is locked in CI — validator schema key-derived from the connector contract, writer pinned to reader at compile time",
      "body": "Triggered by a production incident investigation: openalex and semantic-scholar showed not_connected on the deployed Control Center. The trace ruled out every structural suspect — corpora/evidence/manifest.json is tracked in main (no .gitignore exclusion), it validates against the manifest contract (source_ids present, last_run present), and the Vercel production deployment was built from the exact main HEAD sha. Root cause: the last committed connector run had status \"partial\" (all Semantic Scholar queries hit keyless HTTP 429, D-018), and computeSourceState treats a source as connected only when a manifest lists it in source_ids with last_run.status \"success\" — run_history successes do not count, and the corpus-level status flips ALL sources the corpus harvests, so OpenAlex went red alongside Semantic Scholar despite succeeding in that same run. The showcase honestly rendered degraded data; no code defect. But the investigation exposed a real latent risk: the manifest contract exists in three hand-maintained mirrors — writer (tools/connectors/types.ts Manifest), reader (lib/types.ts CorpusManifest, which lib/status.ts computes source states from; lib never imports tools), and the Ajv schema inside tools/validate.ts. A connector refactor could change the written shape, update one mirror, and validation would stay green while statuses silently flipped. Guard added in tools/validate.ts: (1) the Ajv schema's properties and required keys are now derived from the writer contract via TypeScript satisfies constraints (Record<keyof Manifest, unknown> etc.) — a field renamed, removed, or added without a schema update no longer compiles; (2) a type-level assertion pins Manifest to CorpusManifest, so the writer can never stop satisfying what status computation reads; (3) manifest discovery is now recursive — every /corpora/**/manifest.json at any depth is validated, with internal (\"_\"-prefixed) dirs recognized at any level. Both npm run validate and the type check inside npm run build run in CI on every push, so contract drift turns the build red instead of flipping sources to not_connected on production. The incident status itself needs no code fix: a success run (reliable with S2_API_KEY, D-018) restores connected; per-source run granularity remains a possible future refinement of D-014.",
      "area": "process"
    },
    {
      "id": "D-021",
      "date": "2026-07-13",
      "title": "Health is a separate axis from connection — a 6-hourly heartbeat file, probes restricted to the D-011 whitelist, stale renders unknown",
      "body": "Connection (computed from corpus manifests, D-013/D-014: has a harvest run ever succeeded) says nothing about whether an API is answering right now, so the two are now independent axes. tools/health-check.ts (npm run health) performs ONE minimal single-record request per probeable api source — one attempt, no retries, because retries would mask throttling — and writes /corpora/_health/heartbeat.json ({generated_at, entries[]: {source_id, checked_at, status, latency_ms, note}}). Status mapping: ok = 2xx; degraded = HTTP 429/206 (the s2_throttled condition, D-018); down = network error, timeout, or 5xx. Probes are restricted to the D-011 whitelist: openalex, semantic-scholar, pubmed-europepmc, wayback-cdx get probes; app-store-reviews, sec-edgar, google-patents get status unknown with a no-probe note — the whitelist grows when a connector for a source actually gets built, not ahead of it (probing hosts we do not harvest is surface without purpose). ventora-telemetry (internal) is n_a — no external endpoint by design; unknown would read as a problem. Scheduling: .github/workflows/connectors.yml runs the check every 6 hours plus workflow_dispatch and commits (data: prefix) only when git sees a diff. Commit-noise rule, resolved in the script rather than the workflow: the file is rewritten only when the per-source status vector changed OR the committed heartbeat is older than 11 hours — the 11h refresh keeps a quiet heartbeat inside the UI's 12h stale threshold at ~2 commits/day worst case. UI: /sources and /connectors render two independent indicators per api/internal source — connection (from manifests, unchanged) and health (from the heartbeat, with checked-Nh-ago); a heartbeat older than 12h renders as unknown, never as ok. The app performs NO live external calls — health is read from the heartbeat file only (constitution invariant 12 stands: no runtime external fetches from app/). Contract safety mirrors D-020: lib/types.ts carries the reader (HeartbeatFile), tools/health-check.ts the writer, and tools/validate.ts pins writer to reader with an AssertAssignable guard plus a key-derived Ajv schema validated when the file is present; every entry's source_id must exist in sources.json. A down source is a recorded fact, not a script failure — the script exits non-zero only on script errors. Runbook (tools/README.md): connected+down = wait/retry later; connected+degraded = check rate limits/keys; not_connected+ok = no successful harvest run exists — run the connector.",
      "area": "operations"
    },
    {
      "id": "D-022",
      "date": "2026-07-13",
      "title": "Operations completion — a manifest cost block, a monthly cockpit rollup, and retry-then-issue failure automation; the operator watches issues, not logs",
      "body": "Closes the operations loop on top of the D-021 heartbeat. (1) Cost accounting: every ManifestRun gains an optional cost block { api_calls, duration_s, tokens_in, tokens_out, estimated_usd } (tools/connectors/types.ts, mirrored in lib/types.ts CorpusManifestCost). api_calls is counted at the polite-fetch layer (tools/connectors/lib/http.ts now returns { fetch, stats } and increments on every outbound attempt INCLUDING retries — the honest request cost), duration_s mirrors the run duration, and estimated_usd is COMPUTED, not asserted: the D-011 whitelist is entirely free public APIs so a pure-fetch run computes to 0, and it becomes non-zero only when a priced job reports token usage. tokens_in/tokens_out are RESERVED for future LLM jobs and written null until an engine exists (invariant 5: no engine, no LLM calls this phase). cost is optional on the run because runs recorded before D-022 carry no block and rewriting history would be dishonest — the runner (tools/run-connector.ts) always writes it going forward. The validator schema is key-derived from ManifestCost via satisfies (same D-020 drift guard: all five keys required inside the block, tokens typed integer-or-null, additionalProperties false), so the writer, reader, and Ajv schema stay pinned at compile time. (2) Monthly rollup: lib/status.ts computeMonthlyRollup aggregates each manifest's run_history for the current UTC calendar month into per-connector rows plus a total (runs, api_calls, duration, estimated_usd); runs with no cost block contribute 0 to the money/call figures but still count as runs. The /connectors cockpit renders a MonthlyRollupPanel above the corpus panels with an honest empty state and the caveat that run_history is capped at RUN_HISTORY_LIMIT (20) per connector, so the rollup covers retained runs only. No number or status is hardcoded — everything is derived from manifests. (3) Failure automation: .github/workflows/connectors.yml retries a failed run once after a 60s backoff; on persistent failure it opens a GitHub issue titled connector:{id} failing, labeled ops, with the captured error output, then fails the run red. The ops label is created if missing, and creation is deduplicated by exact title among open ops issues — never a second issue while one is open; closing the issue after a fix re-arms the automation. The step is written generically around CONNECTOR_ID (today the health job, id health; a future scheduled harvest job reuses it verbatim and would paste last_run.error). The workflow uses the standard Actions token (issues: write) — no new external endpoint, so invariant 12 (which governs tools/ scripts) is untouched and no LLM calls are introduced. (4) Runbook (tools/README.md, Operations section): the operator's contract is watch issues, not logs — a green issue tracker means healthy operations; nobody reads Actions logs proactively.",
      "area": "operations"
    },
    {
      "id": "D-023",
      "date": "2026-07-13",
      "title": "Constitution amendment: a narrow git-backed write surface for the Operations UI",
      "body": "The app was strictly read-only (rule 12: no runtime external fetches from app/, including server actions; rule: no data-editing forms in the UI). This amendment opens a deliberately NARROW write surface so a non-technical operator can run the fleet without touching git. The rules change in three exact ways. (1) rule 1 (source of truth) now lists /corpora/_ops/**.json as operational config that the app MAY write; all other data — registry, dossiers, docs, sources, and decisions except the auto-entries below — stays read-only in the UI and is edited only in git. (2) rule 12 gains ONE external-fetch exception: server actions under app/ops/ MAY call api.github.com to (a) commit the allowlisted _ops write surface via the Contents API, (b) dispatch harvest workflows via workflow_dispatch, (c) poll a run, and (d) download a run's quote artifact — nothing else. The app never harvests; it only triggers the GitHub workflow that does. All other app/ code remains file-based with zero runtime external calls, and LLM API calls remain forbidden everywhere. (3) the no-UI-forms rule gains the same narrow exception. Enforcement is not by convention: every write funnels through one server-action guard with a hard allowlist — the exact path corpora/_ops/budget.json, or corpora/_ops/connectors/{id}.json where {id} matches ^[a-z0-9-]+$ AND is a registered connector, plus a single append-only operation on decisions/decisions.json that may only add a well-formed auto-entry (next D-XXX id, area operations). Any other path throws before a byte leaves the process. Auth is unchanged (D-006): the whole app, /ops included, sits behind the single-password middleware. The write token is a fine-grained, repo-scoped GitHub PAT read from env GH_OPS_TOKEN (never committed); without it the /ops page renders read-only with an honest empty state and every control disabled. Overrides (raising a cap for one run) are themselves logged: the same action appends an auto-entry to decisions.json, so the decision log stays the single source of truth for deviations — a cap was raised is a decision, not a silent flag.",
      "area": "architecture"
    },
    {
      "id": "D-024",
      "date": "2026-07-13",
      "title": "Operational config lives in /corpora/_ops — budget caps and per-connector limits/pause/cadence/targets, validated in CI",
      "body": "The fleet's operating parameters become data, not code or hidden defaults. /corpora/_ops/budget.json holds { monthly_caps: { usd, calls } } — the ceiling the scheduler respects before starting new runs. /corpora/_ops/connectors/{id}.json holds, per connector, { connector_id, paused, paused_reason, cadence { every_days }, limits { max_calls_per_run, max_records_per_run }, targets[] }. targets is the explicit list the machine harvests (for evidence, mechanism ids) — the schedule's scope is what the owner pointed it at, not an accident of which files already exist on disk; it defaults to every mechanism id with a full record in /registry/mechanisms (currently LA-01), and the scheduled run applies a freshness filter on top (a target is only harvested if its registry record changed in the last 7 days, checked via git log). paused connectors are skipped by the scheduled workflow with the stored reason written to the job summary. The _ops directory is _-prefixed so it is ignored by the showcase corpus scan (lib/status.ts) exactly like _health and _dummy. Contract safety mirrors D-020/D-021: the writer/reader types live in tools/connectors/types.ts and lib/types.ts with an AssertAssignable drift guard in tools/validate.ts, and a SINGLE set of validators in lib/ops.ts is shared by both CI (npm run validate) and the server-action write path — so the UI can never push a config that would redden CI. Validation is strict: required fields, non-negative numbers, filename must equal connector_id, connector_id must be a registered connector, and every target must exist in /registry/mechanisms. Malformed _ops config fails the build rather than silently misconfiguring the fleet.",
      "area": "operations"
    },
    {
      "id": "D-025",
      "date": "2026-07-13",
      "title": "Run-with-quote flow: dry-run dispatch estimates cost before a real run; over-budget requires a logged override",
      "body": "Runs are triggered from /ops, never executed by the app. The flow is: click Run -> a server action dispatches the harvest workflow with dry_run=true and a generated dispatch_id echoed into the run-name for correlation -> the app polls (every 5s, up to ~60s) for the run and downloads its quote.json artifact -> it displays the quote (calls, records, duration, estimated_usd) plus a budget snapshot (month-to-date, caps, remaining, would_exceed) -> the operator confirms -> a second dispatch runs the real harvest. The quote is DETERMINISTIC and makes zero harvest API calls: tools/run-connector.ts gains a `quote` subcommand backed by an optional connector.quote(params) method (evidence estimates from the record's evidence_terms, pins, per-term caps, and the 1s polite interval) and merges a budget snapshot from tools/ops-gate.ts. If the quote exceeds remaining budget, confirmation is blocked until the operator ticks an explicit 'raise cap for this run' checkbox; ticking it makes the server action append a decisions.json auto-entry recording the override before the real dispatch, and passes raise_cap=true to the workflow (which bypasses ONLY the budget gate, never the per-run limits). Quotes are ephemeral estimates, not knowledge — they return as a run artifact and are never committed, so quote spam cannot bury meaningful data in git history. Polling is bounded: if the quote is not ready within ~60s the UI stops and shows 'quote pending — check the run' with a link to the Actions run, never hanging. The scheduled fleet run reuses the same tools/ops-gate.ts gate (paused, cadence-due, per-run limits against the estimate, and budget) and writes every skip reason to the job summary; the D-022 retry-then-issue automation is reused verbatim for real runs.",
      "area": "operations"
    },
    {
      "id": "D-026",
      "date": "2026-07-13",
      "title": "Connection means \"set up\", not \"last run succeeded\" — run quality becomes its own computed axis alongside health",
      "body": "The owner's reading of the /sources board is: connection answers \"have we already set up this source\" (we have 35 sources — the board must show which ones are wired), a second signal answers \"is it working well\", and health (D-021) answers \"is the API accessible right now\". The old rule conflated the first two: connected required a manifest listing the source with last_run.status \"success\", so one partial run (e.g. the keyless S2 429 harvest, D-020) flipped an already-set-up source to not_connected — set-up state and run quality fought over one pill. Redefinition in lib/status.ts computeSourceState: an api/internal source is connected iff ANY /corpora/{dir}/manifest.json lists it in source_ids, regardless of run status — a manifest only exists after a connector was built and run at least once, so its presence IS the file-based set-up proof (a connector merely registered in code but never run stays not_connected, because nothing on disk can prove it). A report source is ingested iff a manifest lists it AND a data_files entry exists on disk (the success requirement dropped for the same reason — the artifact is either on disk or not). New axis: computeSourceLastRun returns the newest last_run (status/timestamp/corpus) across manifests listing the source, rendered through the existing RUN_STATUS_META (success emerald / partial amber / failed red) as a \"last run\" column on /sources and /connectors; granularity stays per corpus (D-020) — all sources one corpus harvests share its run status, and per-source run granularity remains the future D-014/D-020 refinement. Health is untouched. Effect on current data: openalex and semantic-scholar render connected + last run partial + health degraded (truthful: set up, last harvest throttled, currently throttled); wayback-cdx and pubmed-europepmc render not_connected + no run + health ok (truthful: API fine, connector not built); the overview mode count becomes 2/7 connected. No schema or validator change — manifests are untouched and everything is recomputed at render time. Docs rewritten to the three-axis rule: SPEC §4, sources/README, corpora/README, and the tools/README runbook matrix (connection × last run × health).",
      "area": "data"
    },
    {
      "id": "D-027",
      "date": "2026-07-13",
      "title": "Semantic Scholar rate compliance — one global 1 rps queue per process, 2s backoff on residual 429s, fetch-layer enforcement of the _ops run budget, workflow-level concurrency group",
      "body": "The issued Semantic Scholar key allows 1 request/second CUMULATIVE across ALL endpoints — search, paper details, references, and health probes draw from one allowance, so per-endpoint pacing is non-compliant by construction. Compliance is enforced structurally in the shared fetch layer, not by connector discipline. (1) Global queue: tools/connectors/lib/http.ts gains enqueueS2(), a single per-process promise chain that serializes every request to api.semanticscholar.org at ≥1100ms spacing (the 100ms margin absorbs clock skew against S2’s rate counter); createPoliteFetch routes S2 URLs through the queue automatically, and tools/health-check.ts routes its S2 probe through the same queue — S2 calls are never parallelized, and the rule for future tools is: no S2 call outside createPoliteFetch or enqueueS2(). (2) Residual 429s: a 429 despite the limiter retries with exponential backoff starting at 2s (2s, 4s; other hosts stay at 1s, 2s; Retry-After still wins when present) and the evidence connector records warnings: { s2_throttled: true } on ANY S2 429 — keyed or keyless — while the keyless per-term batch reduction from D-018 stays keyless-only. (3) Run budgets: the runner reads the connector’s /corpora/_ops/connectors/{id}.json (contract D-024, shared loaders in lib/ops.ts) and passes limits.max_calls_per_run into createPoliteFetch, which fails the next request once the budget (retries included) is spent — the D-025 pre-run quote ceiling is thereby also enforced DURING the run at the fetch layer, and a connector cannot opt out. The evidence default drops from 500 to 150: sized so the worst case (every call S2) stays under ~3 minutes at 1 rps; a normal run uses ~25–50 calls. Reference snowballing deliberately stays on OpenAlex only: it carries the same citation graph without the 1 rps ceiling, so reference expansion never spends S2 budget. (4) Cross-process concurrency: .github/workflows/connectors.yml previously had NO concurrency group — verified, not assumed — and gains a workflow-level group connectors-s2 (cancel-in-progress false) covering every job in the file, the health heartbeat today and any future scheduled harvest job; any connector workflow living in a separate file (e.g. harvest.yml) must reuse the exact group name so two runner processes never call S2 simultaneously. Within one job the heartbeat’s S2 probe and any connector S2 calls share the process queue; as separate scheduled jobs they now queue behind each other instead of colliding. (5) Runbook (tools/README.md): S2 = 1 rps cumulative per key; do not add S2 calls to any new tool without routing through the shared limiter. Evidence connector bumped to 2.1.0.",
      "area": "operations"
    },
    {
      "id": "D-028",
      "date": "2026-07-14",
      "title": "Connector #2 (wayback) — quarterly Wayback CDX capture index per survivor domain; URLs and dates only, no page contents",
      "body": "The interfaces corpus starts as a time-lapse INDEX of surviving products' web surfaces, not a content archive. tools/connectors/wayback.ts (sourceId wayback, source_ids [wayback-cdx]) makes ONE CDX API call per domain — /cdx/search/cdx with output=json, from/to year bounds (default 2015-2026), filter=statuscode:200, collapse=timestamp:6, fl=timestamp,original — and reduces client-side to the FIRST capture of each quarter (CDX collapse can express months via the YYYYMM timestamp prefix but not quarters, so the server collapses to ≤1/month and the connector keeps ≤4/year). Output: /corpora/wayback/{domain}.json { domain, path, fetched_at, years, captures[]: { date, wayback_url, original_url } }. Page contents are DELIBERATELY not downloaded at this step — a full-page corpus would blow the 40 MB corpus guardrail immediately; content harvesting is a later, size-conscious step against this index. The domain list is owner-editable DATA, not code: tools/connectors/wayback-domains.json ships with 10 survivors (duolingo.com, calm.com, notion.so, canva.com, headspace.com, strava.com, grammarly.com, dropbox.com, spotify.com, blinkist.com); a domains=\"a.com;b.com\" param overrides for ad-hoc runs, path=/onboarding scopes the query to a sub-surface, years=YYYY-YYYY bounds the range. Failure handling mirrors evidence.ts: a failing domain is logged and skipped (run status partial with joined errors), the run throws only when every domain failed, and a domain with zero archived captures still writes an honest empty index — the archive having nothing is a finding, not an error. Ops config corpora/_ops/connectors/wayback.json: cadence 90 days, max_calls_per_run 40, max_records_per_run 2000, and targets [] DELIBERATELY — the D-024 target machinery is mechanism-centric (targets validate against registry mechanism ids and freshness reads registry git history), while domains are connector data in wayback-domains.json; scheduled runs therefore skip with the honest \"no targets configured\" reason and harvests are triggered manually or from /ops (blank target dispatches the full list). Wiring domains into the scheduler is a separate future decision. quote() (D-025) is deterministic: calls = domains, records = domains × years × 4 quarters (upper bound), duration ≈ calls × 1s polite interval, estimated_usd 0 (CDX is free, D-011).",
      "area": "data"
    },
    {
      "id": "D-029",
      "date": "2026-07-14",
      "title": "Report ingest tool — tools/ingest-report.ts normalizes owner-prepared benchmark tables into the /corpora/benchmarks corpus; report sources need a normalizer, not a connector",
      "body": "Report-mode sources (connection_mode \"report\", D-013 — revenuecat-report, unbounce-benchmarks, goodui, product-analytics-benchmarks, forrt) live in published PDFs a human downloads and reads: there is nothing for a connector to fetch (the D-011 whitelist does not cover them, and gated report PDFs are not scrapable — rule 12). The other half of the pipeline is a NORMALIZER: the owner prepares a small table, the tool structures and validates it. tools/ingest-report.ts (npm run ingest -- <source_id> <file.(csv|json|md)> [retrieved=YYYY-MM-DD]) validates that <source_id> exists in sources.json AND is connection_mode \"report\", parses the input by extension (.csv with a header row and a quote-aware splitter, .json as an array of row objects, .md as the first GitHub-flavored pipe table), normalizes every row to { metric, category?, value, unit, notes? } with minimal shape validation (metric/unit non-empty strings, value a finite number, category/notes optional), and — one bad row aborts the whole run BEFORE any write, a benchmark corpus is whole or not written — writes /corpora/benchmarks/{source_id}.json = { source_id, retrieved, metrics[] }. It then writes/merges /corpora/benchmarks/manifest.json (corpus id \"benchmarks\") whose source_ids accumulate every ingested report source (D-014: one corpus, several sources; re-ingest is idempotent). The tool makes ZERO network calls (cost.api_calls 0, estimated_usd 0) and touches ONLY /corpora/benchmarks — sources.json stays git-only and statuses stay COMPUTED (D-013/D-023/D-026): computeSourceState flips a report source to \"ingested\" iff a manifest lists it in source_ids AND its data file exists on disk, so ingesting the file IS the status change, nothing is hardcoded. Contract safety mirrors D-020/D-021/D-024: the writer type (BenchmarkFile/BenchmarkMetric in tools/ingest-report.ts) and reader mirror (lib/types.ts) are pinned by an AssertAssignable drift guard in tools/validate.ts, which also validates every /corpora/benchmarks/*.json against the record schema and requires the filename stem to be a report-mode source id. writeManifest was refactored to take a ManifestIdentity ({ sourceId, sourceIds, connectorVersion }) instead of a full Connector so the ingester (which is not a connector) and the connector runner share one manifest writer. Content rule 8 holds: the tool writes code and structure, never numbers — benchmark values come only from the owner's real report extracts. First targets: RevenueCat State of Subscription Apps (trial->paid CVR and retention by app category), Unbounce conversion benchmarks (landing CVR by industry), GoodUI (pattern test win/loss outcomes); the benchmarks corpus backs the future effects table's baseline column.",
      "area": "data"
    },
    {
      "id": "D-030",
      "date": "2026-07-14",
      "title": "Scheduled orchestration consolidated in connectors.yml — heartbeat 6h / evidence weekly / wayback monthly, health-aware skipping, per-connector auto-commit; harvest.yml becomes dispatch-only",
      "body": "Step 6 assembles the final scheduled surface without rebuilding the gate. connectors.yml now owns EVERY scheduled connector job and carries three cron entries routed by github.event.schedule: 0 */6 * * * (heartbeat, D-021, unchanged, also workflow_dispatch), 0 5 * * 1 (evidence weekly), 0 6 1 * * (wayback monthly, 1st 06:00 UTC). The weekly scheduled job was MOVED out of harvest.yml, which is now dispatch-only (quote + confirmed run from /ops, D-025); its schedule trigger and scheduled job were deleted and the header rewritten. Both files keep the workflow-level concurrency group connectors-s2 (cancel-in-progress false, D-027), so the monthly wayback run colliding with the 06:00 heartbeat tick on the 1st serializes instead of racing S2, and manual dispatches are covered by the same guarantee. A new generic harvest-scheduled job is parameterized by CONNECTOR_ID (picked from the schedule: wayback on the monthly cron, else evidence) and reuses the existing patterns verbatim — fetch-depth 0 for the freshness git log, ops-gate plan for the decision, the D-022 retry-once-then-deduplicated-issue automation, and honest 'commit only when git sees a diff'. Every scheduled path already respects paused (with reason), per-run limits (pre-run quote gate + fetch-layer enforcement, D-027), the monthly budget (skip with a logged 'gate blocked' reason, raise_cap never passed on the schedule), and the S2 global queue — verified wired for scheduled context in tools/ops-gate.ts plan (which only the scheduled jobs call), NOT duplicated. Three additions to the gate, no rewrite: (1) plan takes an optional connectorId so each schedule plans only its own connector; (2) HEALTH-AWARE SKIP — planForConnector reads /corpora/_health/heartbeat.json and, if any source the connector harvests (connector.sourceIds) is status 'down' within the HEARTBEAT_STALE_HOURS window (12h, reusing lib/status.ts), skips the whole connector with reason 'source {id} down at HH:MM' (UTC) instead of hammering a dead API into a red run; a missing/stale heartbeat never claims down (the run proceeds and its own retry/issue automation handles a real outage) and 'degraded' does not skip (connectors degrade gracefully on S2 429s, D-027); (3) WAYBACK SCHEDULABILITY — the mechanism-centric target machinery (targets validate against registry mechanism ids and freshness reads registry git history) does not fit wayback, whose scope is the owner-editable wayback-domains.json (D-028). Rather than force fake targets, the Connector contract gains an optional schedulableWithoutTargets flag (set on waybackConnector); when true and _ops targets is empty, plan emits a single default-scope entry (target null, params {}) that still passes cadence, health, and the quote/budget gate — every other connector keeps the honest 'no targets configured' skip. wayback keeps _ops cadence.every_days 90, so the monthly schedule is the CEILING and ops-gate still honestly skips 'not due' until 90 days elapse, matching the quarterly-index design (D-028). Auto-commit for scheduled harvests changed from the flat 'data: scheduled harvest' to 'data(connector): {id} run {YYYY-MM-DD}' so the git log names which connector produced each change. Job summary per run appends the plan's skipped-with-reasons list plus the connector manifest last_run stats (status, records, calls, duration_s, estimated_usd), guarded by a 'ran' output so a nothing-due week never reports a stale manifest as this run. No schema or validator change: the gate reuses existing shared loaders, statuses stay COMPUTED, and the heartbeat/manifests are untouched.",
      "area": "operations"
    },
    {
      "id": "D-031",
      "date": "2026-07-14",
      "title": "The /docs library gains an operator runbook — docs/connectors-runbook.md, rendered alongside the five foundation documents",
      "body": "Step 8 adds docs/connectors-runbook.md: a plain-language operator's manual for the source-connector fleet, written for a non-technical owner and the August team. It documents the two harvest paths (schedule via connectors.yml; the /ops Run button with dry-run quote confirmation via harvest.yml, D-025) and explicitly separates harvesting from guarding — push CI (validate.yml) validates knowledge and manifests but never fetches, so the earlier 'three ways runs happen' framing is corrected to two harvest paths plus a guard layer. It also covers the connection-vs-health status axes and their four combinations (D-021/D-026), the budget model and logged cap-override flow (D-023/D-024/D-025), the per-source cadence table and the S2 1-rps cumulative shared-queue rule with OpenAlex-only snowballing (D-027), the evidence corpus rules including the dissent gate (D-015/D-017/D-019), the 40 MB Postgres-escalation guardrail and no-raw-HTML rule (D-028), the retry-once-then-deduplicated-issue failure policy and 'watch issues, not logs' contract (D-022), the ordered checklist for adding a new connector, and the licensing rule that connection_mode manual sources are never machine-harvested. Wiring: DOC_SLUGS in lib/data.ts gains 'connectors-runbook' so it renders at /docs/connectors-runbook via the existing app/docs/[slug] route with zero new code — operators read it inside the app. This extends the /docs set beyond the SPEC §3.6 foundation five; the distinction is preserved in the code comment and docs/README (the first five are the owner-authored foundation set, the runbook is an operational manual maintained alongside the ops layer). No schema, validator, or content-rule change: the file is owner/ops Markdown like every other /docs file, and the app still only renders docs, never edits them (rule 1). Every quantitative claim in the runbook (monthly caps $5/20000, per-run limits 150/40, cadences 7d/90d, 12h heartbeat staleness, 40 MB guardrail, 1100ms S2 spacing) is copied from and labelled with the config/code file that owns it, so the runbook cites the source of truth rather than becoming a second one.",
      "area": "process"
    },
    {
      "id": "D-032",
      "date": "2026-07-15",
      "title": "First dossier (DOS-LA-01, incubating) + dossier schema carries prose — per-axis markdown rationale, a dissent statement, and a core_condition inside the JSON (Option A, one projection)",
      "body": "The first admission-gate dossier lands for LA-01, and the dossier contract is extended so the scientific reasoning lives IN the record rather than being discarded. Option A (one source, projections) is chosen over building a separate markdown pipeline (rejected B/C): JSON stays the single source of truth and /dossiers renders it — no second file type, no md loader/route to maintain for one record kind. Schema changes in dossiers/dossier.schema.json: (1) each of the five scoring axes changes from a bare integer to an object { score: int 0–3, rationale: markdown string } via a new $defs.axis, so the owner's justification for every score is part of the record; (2) a top-level dissent: markdown string is REQUIRED — every dossier must state its counter-evidence, mirroring the D-019 evidence-corpus dissent gate at the decision layer; (3) a top-level core_condition: string states the measured condition for promotion to CORE (measured, not assumed); (4) evidence_sources changes from string[] to { ref, doi? }[] so citations carry resolvable DOIs. The reader mirror (lib/types.ts: DossierAxis, DossierEvidenceSource, updated DossierScores/Dossier) and the validator follow: tools/validate.ts now sums scores[*].score (not the axis objects) for the total-equals-sum check; the D-019 checkDossierDissent gate (referenced evidence corpus must exist, be classified, and have a non-empty dissent category) is unchanged and satisfied by /corpora/evidence/LA-01.json (dissent category count 26). The /dossiers page renders the rationales and dissent through the existing react-markdown + remark-gfm pipeline already used by /docs (no new dependency, rule against future deps honored), plus a DOI-linked sources list; the empty-state copy is de-staled since a dossier now exists. dossiers/LA-01.json is owner-approved content committed verbatim (DOS-LA-01, verdict incubating, total 13, evidence 3 / product_applicability 3 / measurability 3 / orthogonality 2 / safety 2 — clears total ≥ 11, evidence ≥ 2, safety ≥ 2); code did not author any score, rationale, or claim (rule 8). The registry record registry/mechanisms/LA-01.json sets dossier_ref: \"dossiers/LA-01.json\" (was null). Statuses stay COMPUTED: lifecycle_status was already 'incubating' and the dossier now backs it; promotion to CORE remains blocked on core_condition (a recorded measured effect), never hardcoded.",
      "area": "data"
    },
    {
      "id": "D-033",
      "date": "2026-07-15",
      "title": "Batch 1 harvest prep — seed sub-schema widened for owner evidence_terms/pinned_evidence, EN-03/VR-02/ZE-07 added to evidence targets, and a pin_unresolved output flag so a dead pinned DOI is a finding, not a run failure",
      "body": "Preparing the first multi-mechanism evidence harvest (Batch 1) required three small, aligned changes and no new machinery. (1) SCHEMA: registry/mechanism.schema.json $defs.seedStub gains two OPTIONAL properties — evidence_terms (array of non-empty strings) and pinned_evidence ({ title, doi, reason }[], additionalProperties false) — reusing the exact shapes the full L1 record already defines. The stub stays closed (additionalProperties false) and stays a stub: neither field is added to required, id/name/grade_draft/oneliner/parent/lifecycle_status are untouched, and rule 8/9 holds — a candidate can now be HARVESTED before it is fleshed out without inventing any mechanism content. The evidence connector already read both fields from seed stubs (loadMechanism falls back to _seed/{id}.json; MechanismRecord already declared them), so only the validator's seedStub gate was blocking owner-provided terms/pins on a stub. (2) DATA: EN-03, VR-02, ZE-07 seed stubs receive owner-provided evidence_terms and pinned_evidence verbatim (endowment effect, variable reward, progress/incompleteness respectively — each pin set includes a dissent/boundary entry per D-015/D-019 intent), and corpora/_ops/connectors/evidence.json targets grows from [LA-01] to [LA-01, EN-03, VR-02, ZE-07]. validateOpsConnectorConfig checks each target against the registry roster (seed ids included), and each per-run quote stays well under the config max_calls_per_run 150 (EN-03 heaviest at ~17 calls). (3) BEHAVIOR: tools/connectors/evidence.ts fetchPinnedWork now sets pin_unresolved: true on the fallback record returned when a pinned DOI does not resolve on OpenAlex (both the null-record and catch paths). The run already never threw for a failed pin — the pin entered the corpus with owner-provided metadata — this only makes the failure VISIBLE in the output so the owner can correct the DOI; a resolved pin (success path) carries no flag, and a pin overlapping a harvested search record stays unmarked. pin_unresolved is an additive optional field on EvidenceRecord, so no reader contract breaks. No harvest is triggered here — this is prep only; owner/ops triggers the actual runs.",
      "area": "data"
    },
    {
      "id": "D-034",
      "date": "2026-07-15",
      "title": "Snowball anchors must be topically validated, not just highly cited — a wrong anchor is worse than no anchor",
      "body": "The snowball completeness check (D-019) selected its review anchors as the top-2 highest-cited review-like records in the corpus with NO topical gate, so a broad, high-citation review sharing one keyword with the mechanism's search terms (a dopamine/default-mode review for loss aversion, a c-fos or animal-numerical-cognition paper for Zeigarnik/endowment) was chosen as the anchor. Its references never overlapped the mechanism's corpus, yielding coverage 0 and snowball_added 0 while masquerading as a real completeness signal. Fix in tools/connectors/evidence.ts (connectorVersion 2.1.0 → 2.2.0): a review now qualifies as an anchor only if it passes all three gates — (a) TOPICAL, a distinctive core keyword from the mechanism's evidence_terms appears in the review's TITLE and ≥2 distinct core keywords appear in its ABSTRACT (isTopicalReview + coreKeywords, which tokenizes evidence_terms and drops a generic academic stoplist — effect(s), meta, analysis, replication, gap, hypothesis, motivation, willingness, accept, theory, model, review, systematic — plus tokens <4 chars, so a single collision word cannot qualify and a record with no abstract cannot be validated); (b) TYPED, openalex_type is 'review' or the title matches the meta-analysis/systematic-review regex (unchanged isReviewLike); and (c) SUBSTANTIVE, referenced_works_count ≥ SNOWBALL_MIN_REFERENCES (20) — a real review, not a commentary. referenced_works_count was added to OPENALEX_SELECT and to the OpenAlexWork/EvidenceRecord shapes (mapped in fromOpenAlexWork, carried through mergeCluster) so candidates are gated from the search results without an extra call. When zero reviews qualify the connector writes coverage_report.review_found: false with reviews: [], snowball_added: 0, and an honest note, rather than picking a garbage anchor; review_found: true otherwise. review_found is an additive field on CoverageReport — no evidence-output JSON schema, validator, lib/types mirror, or app reader consumes coverage_report, so no contract breaks. Re-ran the three Batch 1 mechanisms: VR-02 (now the full 6 evidence_terms + terms_source 'record' + both pins after the stale 2026-07-13 pre-D-033 file was overwritten — the connector has no cache, the file was simply old) found valid dopamine-reward review anchors; EN-03 honestly wrote review_found: false (its endowment meta-analyses were S2-sourced without referenced_works_count, or OpenAlex reviews with only one core keyword in the abstract); ZE-07 kept one topical goal-theory review anchor (coverage 0, honestly reported) instead of the prior collision anchor. This is a process rule going forward: snowball completeness may only be anchored on topically-validated reviews, and an honest empty state is required when none exists.",
      "area": "process"
    },
    {
      "id": "D-035",
      "date": "2026-07-15",
      "title": "Mechanism schema vocabulary widened to admit owner-approved records — refined evidence grades (+/-) and richer relation types (enables, orthogonality_note)",
      "body": "Admitting the Batch 1 owner-approved full records (EN-03, VR-02) surfaced two places where the mechanism.schema.json vocabulary was narrower than the approved content, and the owner directed that the content is authoritative — the schema is extended via this decision rather than the science being edited (.cursorrules #8 forbids touching owner science; #4 forbids weakening the schema for CONVENIENCE — this is fidelity to approved content, done through the sanctioned decision path #7, not a workaround). (1) EVIDENCE GRADE: registry/mechanism.schema.json evidence.grade enum grows from [A, B, C] to [A+, A, A-, B+, B, B-, C+, C, C-]. EN-03 carries a deliberate A- (\"Grade held at A- (not A) because interpretation and magnitude are genuinely contested\" — Plott & Zeiler procedure critique, List market-experience attenuation), a scientific judgment the coarse three-grade scale could not express. lib/types.ts EvidenceGrade is widened to the same nine-value union. The seedStub keeps its free-form grade_draft, untouched. (2) RELATION TYPES: relations[].type enum grows from [enabled_by, adjacent, hybrid_with] to [enabled_by, enables, adjacent, hybrid_with, orthogonality_note]. 'enables' is the inverse direction of the existing 'enabled_by' (EN-03 enables LA-01; LA-01 already encodes the mirror enabled_by → EN-03), and 'orthogonality_note' records a managed-overlap annotation between mechanisms (EN-03's 2/3 orthogonality vs LA-01, cross-referenced to its dossier). lib/types.ts RelationType is widened to match, and tools/render-cards.ts relationLabel gains cases 'enables' → \"enables\" and 'orthogonality_note' → \"orthogonality note\" so the card generator's exhaustive switch stays total. No app reader breaks: app/registry/page.tsx renders evidence.grade and relation.type as free strings (no colour map or exhaustive switch keyed on either), so the wider vocabulary renders as-is with no hardcoded status. The HARD RULES (non-empty implementations[].metrics and constraints.hard_rules) and every other constraint are unchanged.",
      "area": "architecture"
    },
    {
      "id": "D-036",
      "date": "2026-07-15",
      "title": "Batch 1 records EN-03, VR-02 admitted to incubating via dossiers",
      "body": "The first batch of owner-approved full L1 records is admitted, moving the registry from 1 to 3 full records (LA-01, EN-03, VR-02) and the dossier count to 3. EN-03 (Endowment effect, parent S5, grade A-) and VR-02 (Variable reward, parent S2, grade A) each replace their _seed candidate stub (the stubs are removed so no duplicate id enters the roster), and their dossier_ref fields point to the corresponding dossiers/{id}.json admitted alongside them. Both records validate against the full mechanism.schema.json (EN-03 only after D-035 widened the grade and relation vocabularies; VR-02 validated as-is) and pass the HARD RULES; both dossiers validate against dossier.schema.json with correct axis-sum totals (EN-03 14, VR-02 13) and rest on evidence corpora with non-empty dissent categories (D-019: EN-03 dissent 32, VR-02 dissent 27). Lifecycle status is incubating for both, matching each dossier verdict. VR-02's safety axis is deliberately the lowest in the set (1/3) — it is the mechanism closest to addictive design — admitted with four block-severity hard rules and the understanding that its implementations receive the strictest review; the validator enforces schema shape, total = axis sum, roster membership, and dissent coverage, and the incubating gate thresholds remain an owner judgment as documented in the dossier schema. Cards regenerated with npm run cards; npm run validate and npm run build are green.",
      "area": "data"
    },
    {
      "id": "D-037",
      "date": "2026-07-15",
      "title": "Batch 1 record ZE-07 admitted to incubating via dossier",
      "body": "ZE-07 (Progress & incompleteness, parent S3, grade B+) is admitted as a full L1 record, replacing its _seed candidate stub (removed so no duplicate id enters the roster) and moving the registry to 4/12 full records (LA-01, EN-03, VR-02, ZE-07) with 4 dossiers on file. Its dossier_ref points to dossiers/ZE-07.json, admitted alongside it. The record validated against the full mechanism.schema.json as-is — the B+ grade needed the refined-grade enum widened in D-035 (its two relations are plain 'adjacent', already in the base vocabulary), so no further schema change was required. HARD RULES pass (three implementations each with non-empty metrics; four block-severity hard rules). The dossier validates against dossier.schema.json with total 13 = axis sum (evidence 2, product_applicability 3, measurability 3, orthogonality 2, safety 3) and rests on an evidence corpus with a non-empty dissent category (D-019: dissent 25). Evidence is scored 2 by design: ZE-07 is a composite of a strongly field-validated component (goal-gradient / endowed-progress — Kivetz-Urminsky-Zheng 2006, Nunes-Dreze 2006) and a weaker one (the classic Zeigarnik memory effect, mixed replication); the dossier dissent documents this and the implementations lead with the strong components, treating the pure open-loop lever as the most tentative. Lifecycle status incubating matches the dossier verdict. Cards regenerated with npm run cards; npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-038",
      "date": "2026-07-15",
      "title": "Evidence terms/pins live on the full record; corpus reproducibility is a validation requirement; a re-harvest never silently regresses a corpus",
      "body": "Batch 2 harvest prep exposed a data-integrity gap, not just a targets-list choice. Owner evidence_terms/pinned_evidence had lived only on the _seed stubs (D-033); when EN-03, VR-02, ZE-07 were admitted to incubating their stubs were deleted (D-036/D-037) and the harvest fields were NOT copied onto the full L1 records. Because tools/connectors/evidence.ts resolves terms from the record and falls back to [name] when absent (D-015), a re-harvest of any admitted mechanism would silently rebuild its corpus from the bare mechanism name — discarding the disconfirming/boundary terms that made the corpus scientifically usable (D-019) and overwriting the good file. Three aligned fixes, no new machinery. (1) REPRODUCIBILITY IS VALIDATED: tools/validate.ts adds a HARD RULE checked explicitly on top of the schema (matching the metrics/hard_rules pattern) — a full record whose dossier_ref is a non-null string MUST carry a non-empty evidence_terms; otherwise validation fails. A dossier rests on a harvested corpus, that corpus was built from terms, so an admitted mechanism without its terms is non-reproducible. The schema already carries the two OPTIONAL fields on the full record (added with D-033/D-017/D-015); this makes them REQUIRED once a dossier exists, without weakening any schema shape. (2) RESTORE: the exact owner-provided evidence_terms and pinned_evidence for EN-03, VR-02, ZE-07 (recovered verbatim from the pre-admission Batch 1 seed stubs — no new science invented, rule 8) are written onto the full records, so the existing corpora (143/201/139 records) are now reproducible and the new validation rule passes. (3) NO SILENT REGRESSION: evidence.ts (connectorVersion 2.2.0 → 2.3.0, behavior change) now compares the new record count against the existing /corpora/evidence/{id}.json before writing; if the re-harvest produced FEWER records it does NOT overwrite — it writes {id}.regression.json alongside, returns status 'partial' with warnings.regression_suspected true and an error stating old-vs-new counts, and keeps the prior corpus. A first harvest (no existing file) or an unreadable prior file is not a regression (readExistingRecordCount returns null). regression_suspected rides the existing warnings channel (Record<string,boolean>), so RunStatus, the Manifest/CorpusManifest contracts, and the D-020 drift guards are untouched; the harvest workflow's commit-if-changed step commits the side file for review. Also: lib/types.ts SeedStub gains optional evidence_terms/pinned_evidence to mirror the schema's seedStub sub-schema (rule 13), and corpora/_ops/connectors/evidence.json targets grows to all six (EN-03, VR-02, ZE-07, HA-04, FR-11, SC-06) — Batch 1 re-harvests now use their restored terms, and Batch 2 candidates (HA-04, FR-11, SC-06) carry owner terms/pins on their stubs. S2_API_KEY is already set as a GitHub Actions secret (harvest runs in Actions, not Vercel), so keyed S2 runs avoid the keyless 429 degradation (D-018). No harvest is triggered here — prep only; runs are dispatched from /ops.",
      "area": "data"
    },
    {
      "id": "D-039",
      "date": "2026-07-15",
      "title": "/ops Run Now dispatches the target it displays; the quoted target is the single source of truth through Confirm",
      "body": "The /ops evidence Run Now selector was ignored: selecting HA-04 and pressing Run dispatched EN-03, and the selector snapped back to EN-03 after the run. Trace found a client-only state bug in app/ops/ops-client.tsx RunFlow — NOT a break in dispatch, workflow forwarding, or the connector. The chosen target lived in React state initialized once to targets[0]; the connector card edits its targets list live, and when the previously-selected target dropped out of the list the controlled <select> repainted its first option (browser behavior when value matches no option) WITHOUT firing a change event, so state silently kept the stale target. startDryRunAction and confirmRealRunAction then dispatched the invisible stale value while the operator saw the newly-painted first option. Workflow forwarding was correct throughout: dispatchHarvest posts target as a workflow_dispatch input, harvest.yml forwards it identically to both tools/ops-gate.ts check and npm run connector as mechanism=${{ inputs.target }}, and evidence.ts has no target fallback (loadMechanism throws on empty) — an empty target could never become EN-03 downstream. Dry-run and real-run used the same state and the same input naming, so they were consistently wrong together; the estimate only 'looked correct' because QuoteConfirm never displayed which target it priced and EN-03/HA-04 quote to similar figures. Fix, entirely in RunFlow: (1) reconcile the displayed value at render — selected = targets.includes(target) ? target : targets[0] — used for the <select> value, the dry-run dispatch, and the confirm fallback, so state and paint can never diverge; (2) the real run dispatches quote.target (the target echoed back in the quote artifact, lib/ops QuoteArtifact.target), so the ESTIMATE is the single source of truth for the confirmed dispatch rather than a fresh dropdown read; (3) changing the selector while a quote is on screen discards the quote (back to idle) so a quote can never be confirmed against a different target; (4) the estimate header now names its target ('estimate — HA-04'). The selected value persists after a run because state is never reset and can no longer be silently invalidated. No schema, type, workflow, or connector change; QuoteArtifact already carried target. Verified: quote smoke test echoes target HA-04; npm run validate and npm run build green. The connectors-runbook Path B acceptance check gains a regression note and a step to confirm the estimate names the selected target before Confirm.",
      "area": "operations"
    },
    {
      "id": "D-040",
      "date": "2026-07-15",
      "title": "/ops reads the LIVE _ops config from GitHub for display, not the deploy-time filesystem snapshot",
      "body": "The D-039 client fix removed one half of the wrong-target bug (stale React state when the targets list was edited live); production kept dispatching the stale first target (EN-03) because of the other half: /ops WRITES config to GitHub (D-023) but READ it from the local filesystem via lib/ops.loadOpsConnectorConfigFromDisk (readFileSync). On Vercel that file is the deploy-time bundle snapshot; export const dynamic='force-dynamic' re-runs the server component per request but re-reads the SAME frozen file, so an operator's just-saved targets stayed invisible until the next redeploy (~30-60s build lag, plus the already-open tab never reloading). The Run selector then defaulted to the stale list's first target and dispatched it — the write path was live while the read path lagged. Fix: a new read-only server action app/ops/actions.ts loadLiveConnectorConfigsAction() reads every KNOWN_CONNECTOR_IDS config live from GitHub via the existing getRepoFile client, confined to the same _ops allowlist as the write path (isAllowedOpsWritePath) and skipping any unparseable/foreign payload rather than throwing (a bad file must not blank the console). app/ops/ops-client.tsx OpsClient calls it once on mount when the write surface is enabled and bumps a configVersion so each ConnectorCard remounts and reseeds its config state — and the RunFlow selector — from the committed config; when the surface is read-only (no token) or the read fails, the disk snapshot the page rendered with stays in effect. This EXTENDS the D-023 exception to rule 12: server actions under app/ops/ may now also GET (read) the allowlisted /corpora/_ops/** paths from api.github.com for display, in addition to committing them, dispatching harvests, polling runs, and downloading quote artifacts — still nothing outside the _ops allowlist, and still no other host. No schema/type/workflow/connector change. Net effect: the displayed targets, limits, and the Run selector reflect committed config immediately, so estimate/dispatch/connector all act on the target the operator actually sees (closing the deploy-lag path that survived D-039). Verified end-to-end by dispatching evidence HA-04 (dry + real) from the harvest workflow and confirming corpora/evidence/HA-04.json is written with manifest last_run.params.mechanism=HA-04; npm run validate and npm run build green.",
      "area": "operations"
    },
    {
      "id": "D-041",
      "date": "2026-07-15",
      "title": "/ops holds connector cards behind a loading gate until the live config read resolves — never shows the deploy snapshot as if saved",
      "body": "Follow-up to D-040: saves were persisting correctly (evidence.json commits landed in git with the exact targets shown), but operators reported 'different settings when I come back'. Cause: the page server-renders connector config from the deploy-time filesystem snapshot, then the D-040 client read swaps in the live GitHub config on mount. That produced a visible flash of stale settings on load, and if the live read had not yet applied (or silently failed — the first cut did `if (!res.ok) return` and just kept the snapshot), the operator saw the old values with no indication they were stale. Fix in app/ops/ops-client.tsx OpsClient: when the write surface is enabled, config is sourced ONLY from the live read — the connector cards are held behind a short 'loading current settings…' state until loadLiveConnectorConfigsAction resolves, so the snapshot is never displayed as if it were the saved config. A failed read now shows an explicit error with a Retry button (setReloadKey re-runs the effect) instead of silently rendering the stale snapshot. When the surface is read-only (no token), there is no live source, so the snapshot renders immediately as before (status seeded to 'ready'). The BudgetPanel still renders from the snapshot (month-to-date usage is computed from manifests, not the _ops write surface, and is not a 'setting' the operator saves). No schema/type/workflow/connector change; purely the ops console's client render lifecycle. Net effect: the settings shown are always the committed ones or an honest loading/error state — closing the last stale-display path after D-039 (client state) and D-040 (live read). npm run validate and npm run build green.",
      "area": "operations"
    },
    {
      "id": "D-042",
      "date": "2026-07-15",
      "title": "Schema vocabulary widened to admit two owner-approved Batch 2 fields — top-level orthogonality_note on the mechanism record and flag_lowest_admitted on the dossier",
      "body": "Admitting the Batch 2 owner-approved records surfaced two fields the schemas did not yet carry, and — as with D-035 — the owner content is authoritative, so the schemas are extended via this decision rather than the science being edited (.cursorrules #8 forbids touching owner content; #4 forbids weakening the schema for CONVENIENCE — this is fidelity to approved content through the sanctioned decision path #7, not a workaround). Both schema roots are additionalProperties:false, so a field they lack fails validation. (1) MECHANISM RECORD: registry/mechanism.schema.json gains an OPTIONAL top-level orthogonality_note (string, minLength 1). FR-11 (Framing & anchoring) uses it to explain why two distinct cognitive phenomena (framing, anchoring) are bundled as one L1 — a managed-overlap annotation at the record level, distinct from the relations[].type 'orthogonality_note' value (D-035) which annotates a pair of mechanisms. The field is not added to required; the six other full records omit it and stay valid. lib/types.ts Mechanism gains optional orthogonality_note?: string (rule 13). tools/render-cards.ts renders an 'Orthogonality note' section after Telemetry only when the field is present, so owner reasoning is not silently dropped from the generated card projection (D-002) and the six cards without it are unchanged. (2) DOSSIER: dossiers/dossier.schema.json gains an OPTIONAL flag_lowest_admitted (boolean), also not required; lib/types.ts Dossier mirrors it as optional. DOS-SC-06 sets it true to mark scarcity as the lowest-scoring mechanism admitted to incubating (total at the 11 threshold, safety 1 by design). No HARD RULE, total-equals-sum check, dissent gate, or any other constraint changes; the widenings are purely additive optional fields. npm run cards, npm run validate, and npm run build are green.",
      "area": "architecture"
    },
    {
      "id": "D-043",
      "date": "2026-07-15",
      "title": "Batch 2 records HA-04, FR-11, SC-06 admitted to incubating; SC-06 admitted at threshold with strictest guardrails",
      "body": "The second batch of owner-approved full L1 records is admitted, moving the registry from 4 to 7 full records (LA-01, EN-03, VR-02, ZE-07, HA-04, FR-11, SC-06 = 7/12) and the dossier count from 4 to 7. Each record replaces its _seed candidate stub (the three stubs are removed so no duplicate id enters the roster, leaving 5 stubs: CG-05, ID-12, RE-10, SP-08, ST-09), and each dossier_ref points to the dossiers/{id}.json admitted alongside it. All three records carry evidence_terms and pinned_evidence, so the D-038 corpus-reproducibility HARD RULE is satisfied. HA-04 (Habit loop, parent S3, grade A-, dossier total 14) and FR-11 (Framing & anchoring, parent S6, grade B+, dossier total 13) validate against the full mechanism.schema.json — FR-11 only after D-042 added the top-level orthogonality_note it uses to document the framing+anchoring bundle. SC-06 (Scarcity, parent S1, grade B) is admitted AT THE INCUBATING THRESHOLD by design: dossier total 11 (evidence 2, product_applicability 3, measurability 3, orthogonality 2, safety 1) — the lowest-scoring admitted mechanism, flagged flag_lowest_admitted:true (D-042). Its safety axis is 1, tied with VR-02 for the highest-risk mechanism in the set: fake scarcity (invented low-stock counts, resetting timers, fake 'X viewing') is the most common e-commerce dark pattern and is specifically unlawful under EU UCPD/DSA and targeted by the FTC. It is admitted only with the strictest guardrails — three block-severity hard rules (genuine_scarcity_only, no_fake_timers, no_manufactured_limits) plus the has_genuine_limit precondition gate — and its implementations get the strictest review; the dossier documents the reactance/backfire dissent and demotes per-context on measured backfire. All three dossiers validate against dossier.schema.json with total = axis sum and rest on evidence corpora with non-empty dissent categories (D-019: HA-04 44, FR-11 45, SC-06 43). Lifecycle status incubating matches each dossier verdict; promotion to CORE remains blocked on core_condition (a recorded measured effect), never hardcoded. Cards regenerated with npm run cards; npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-044",
      "date": "2026-07-15",
      "title": "Batch 3 harvest prep — owner evidence_terms/pinned_evidence added to the final five seed stubs (CG-05, SP-08, ST-09, RE-10, ID-12) and appended to the evidence targets; prep only, no schema/tooling change",
      "body": "Prep for the third and final multi-mechanism evidence harvest, mirroring D-033/D-038 with zero new machinery. (1) DATA: the five remaining _seed candidate stubs receive owner-provided evidence_terms and pinned_evidence verbatim — CG-05 Curiosity gap (5 terms, 2 pins: Loewenstein information-gap 10.1037/0033-2909.116.1.75, Kang et al. epistemic-curiosity/reward 10.1016/j.neuron.2009.09.031), SP-08 Social proof (6 terms, 3 pins: Asch 10.1037/h0093718, Keizer et al. spreading-of-disorder 10.1126/science.1201362, WOM-on-sales 10.1509/jmkg.73.4.15), ST-09 Status & comparison (5 terms, 2 pins: Festinger social-comparison 10.1177/001872675400700202, status-goods 10.1257/jel.20171300), RE-10 Reciprocity (5 terms, 2 pins: Gouldner norm-of-reciprocity 10.2307/2092623, reciprocity-in-compliance 10.1177/009365002236920), ID-12 Identity & consistency (5 terms, 2 pins: Freedman & Fraser foot-in-the-door 10.1037/h0025589, Oyserman identity-based-motivation 10.1086/651257). No mechanism content invented (rule 8/9): id/name/grade_draft/oneliner/parent/lifecycle_status are untouched and each stays a candidate stub. The seedStub sub-schema already carries the two OPTIONAL fields with the exact {title, doi, reason} pin shape (D-033), so no schema change is needed. (2) TARGETS: corpora/_ops/connectors/evidence.json targets grows from [FR-11, SC-06] to add [CG-05, SP-08, ST-09, RE-10, ID-12]; validateOpsConnectorConfig checks each against the registry roster (seed ids included) and each per-run quote stays under max_calls_per_run 150. (3) UNRESOLVED PINS: no change required — tools/connectors/evidence.ts fetchPinnedWork already sets pin_unresolved:true on the fallback record when a pinned DOI does not resolve on OpenAlex (D-033), so a dead pin is a visible finding in the corpus output, not a run failure; pin_unresolved stays an output-only flag on EvidenceRecord and is deliberately NOT added to the input schema. (4) KEYED RUNS: S2_API_KEY is already set as a GitHub Actions repo secret and wired into harvest.yml / connectors.yml (harvest runs in Actions, not the app), so S2-backed runs come back keyed rather than degrading to the keyless 429 partial path (D-018). No harvest is triggered here — prep only; runs are dispatched from /ops.",
      "area": "data"
    },
    {
      "id": "D-045",
      "date": "2026-07-15",
      "title": "Dossier schema widened to admit the owner-approved flag_completes_core boolean",
      "body": "Admitting the Batch 3 owner-approved dossiers surfaced one field the schema did not yet carry: DOS-ID-12 sets flag_completes_core:true to mark the dossier that completes the 12-mechanism core admission. As with D-035/D-042, the owner content is authoritative, so the schema is extended via this decision rather than the science being edited (.cursorrules #8 forbids touching owner content; #4 forbids weakening the schema for CONVENIENCE — this is fidelity to approved content through the sanctioned decision path #7, not a workaround). dossiers/dossier.schema.json is additionalProperties:false, so a field it lacks fails validation. Change: dossiers/dossier.schema.json gains an OPTIONAL flag_completes_core (boolean), not added to required — the eleven other dossiers omit it and stay valid; lib/types.ts Dossier mirrors it as optional flag_completes_core?: boolean (rule 13). This is the exact additive-optional-field pattern of D-042 (flag_lowest_admitted); the flag is data-only and, like flag_lowest_admitted, is not rendered by the /dossiers page. No HARD RULE, total-equals-sum check, dissent gate, or any other constraint changes. npm run validate and npm run build are green.",
      "area": "architecture"
    },
    {
      "id": "D-046",
      "date": "2026-07-15",
      "title": "Core complete — all 12 L1 mechanisms admitted to incubating via dossiers; batch 3 (CG-05, SP-08, ST-09, RE-10, ID-12)",
      "body": "The third and final batch of owner-approved full L1 records is admitted, completing the core: the registry moves from 7 to 12 full records (LA-01, EN-03, VR-02, ZE-07, HA-04, FR-11, SC-06, CG-05, SP-08, ST-09, RE-10, ID-12 = 12/12) and the dossier count from 7 to 12, and the _seed folder now holds zero candidate stubs — every seed mechanism has passed the admission gate. Each of CG-05 (Curiosity gap, parent S2, grade B+, dossier total 12), SP-08 (Social proof, parent S4, grade A-, total 14), ST-09 (Status & comparison, parent S4, grade A-, total 13), RE-10 (Reciprocity, parent S4, grade B+, total 14), and ID-12 (Identity & consistency, parent S5, grade B+, total 12) replaces its _seed candidate stub (the five stubs are removed so no duplicate id enters the roster) and points dossier_ref at the dossiers/{id}.json admitted alongside it. All five records carry evidence_terms and pinned_evidence, so the D-038 corpus-reproducibility HARD RULE is satisfied, and each validates against the full mechanism.schema.json as-is — the grades (B+/A-) and relation types (all 'adjacent') were already in the vocabulary from D-035, so no mechanism-schema change was needed this batch. All five dossiers validate against dossier.schema.json with total = axis sum and rest on evidence corpora with non-empty dissent categories harvested in the Batch 3 run (D-019: CG-05 41, SP-08 59, ST-09 40, RE-10 29, ID-12 34). ID-12's dossier carries flag_completes_core:true (schema-admitted in D-045) marking it as the record that closes the core. Lifecycle status is incubating for all twelve, matching each dossier verdict; promotion to CORE remains blocked on core_condition (a recorded measured effect from the telemetry loop or public corpora), never hardcoded — the honest status is 12/12 incubating, 0 core. Cards regenerated with npm run cards (12 cards); npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-047",
      "date": "2026-07-16",
      "title": "Product segments are first-class evolving system data — 15 seed segments classify output products, not Ventora",
      "body": "The product-segment axis becomes first-class data the whole layer references, living in /segments/segments.yaml. This is a small constitution amendment: rule 1 (source of truth) now lists /segments/segments.yaml among the git-only data locations, following the D-011/D-023 amendment pattern; it is edited in git only, no UI write surface. The file is { version, segments[] } where each segment is { id, group, definition, status, provenance }. group is one of business-model | form | audience | usage-rhythm; status is active | retired (a retired segment is kept for history, never deleted); provenance is seed-YYYY-MM for the owner seed set, or analyzer | owner for segments added later. CRITICAL framing (rule 8): these segments classify the OUTPUT products Ventora builds (Ventora builds ANY product type), NOT Ventora itself. The 15 owner-provided seed segments carry provenance seed-2026-07 — by business model: transactional-ecommerce, subscription, freemium, marketplace, lead-gen-service, one-time-digital; by form: web-saas, mobile-app, content-media, local-offline-bridge; by audience: b2b, b2c-mass, creator-prosumer; by usage rhythm: daily-habit, occasional-high-consideration. The segment axis EVOLVES: the analyzer or the owner will add new segments later carrying provenance analyzer/owner, so the validator never hardcodes the count. Validation (tools/validate.ts, run in CI on every push): the file must parse as YAML (a yaml devDependency is added — needed now for this validation, not speculative infrastructure), every entry must match an Ajv schema whose property keys are pinned to the reader contract lib/types.ts Segment via a satisfies constraint (same drift-guard pattern as D-020), and ids must be unique. lib/types.ts gains SegmentGroup, SegmentStatus, Segment, and SegmentsFile (rule 13). Renders nowhere yet by design — this step is DATA ONLY; no app screen reads or displays segments, and the honest empty state is simply that no UI consumes them at this step.",
      "area": "data"
    },
    {
      "id": "D-048",
      "date": "2026-07-16",
      "title": "Pack map is the sole hand-authored input to pack generation; packs are otherwise computed projections",
      "body": "The pack pipeline gets its one and only judgment call: /packs/pack-map.yaml, mapping each Development-Plan element type to the mechanisms whose evidence is relevant to it. This is a small constitution amendment following the D-047 pattern: rule 1 (source of truth) now lists /packs/pack-map.yaml among the git-only data locations; it is edited in git only, no UI write surface. The framing is deliberate — mapping mechanisms to product elements is the SOLE human decision here; everything downstream (the packs themselves) is a COMPUTED projection over this map plus the registry, never hand-authored. The file is { version, elements[] } where each element is { id, applies_to, funnel_stage, mechanisms, note? }. id is a unique element-type slug; applies_to is free-text product-surface slugs; funnel_stage is one of the registry applicability vocabulary (cold_acquisition | onboarding | activation | conversion | retention | reactivation); mechanisms is the list of mechanism ids whose evidence is relevant; note is an optional owner annotation. The 11 owner-provided seed elements are entry, onboarding, first-value, core-ux, retention-return, engagement-loops, social-proof-status, paywall-conversion, retention-billing, trust-friction (guardrail-forward), aha-sharing. IMPORTANT: funnel_stage records evidence RELEVANCE, not runtime applicability — an element's stage may differ from a member mechanism's applicability (e.g. entry is cold_acquisition while SP-08/FR-11 exclude that stage at generation time), so the validator deliberately does NOT cross-check element stage against mechanism excluded_stages. Validation (tools/validate.ts, run in CI on every push): the file must parse as YAML, every element must match an Ajv schema whose property keys are pinned to the reader contract lib/types.ts PackMapElement via a satisfies constraint (same drift-guard pattern as D-020/D-047), element ids must be unique, and EVERY referenced mechanism id must resolve to a registry record (checked against rosterIds — a map pointing at a phantom mechanism would silently produce a wrong pack). lib/types.ts gains FunnelStage, PackMapElement, and PackMapFile (rule 13). Renders nowhere yet by design — this step is DATA ONLY; no app screen reads or displays the pack map. npm run validate and npm run build are green.",
      "area": "data"
    },
    {
      "id": "D-049",
      "date": "2026-07-16",
      "title": "Packs generated via render-packs.ts — computed projection over the pack map + registry, never hand-authored",
      "body": "Packs are now GENERATED, following the render-cards pattern (D-002): tools/render-packs.ts (npm run packs) projects /packs/pack-map.yaml (D-048) + the registry (/registry/mechanisms/*.json, /dossiers/*.json) into one YAML datasheet per element at /packs/pack-{id}.yaml. The hand-written packs/pack-paywall-conversion.yaml is the reference format and is now REPRODUCED by the generator (packs are never hand-authored; the hand version survives in git history). The datasheet structure is pinned to that reference: header (pack, applies_to, funnel_stage, version, nature, source) + LAYER 1 mechanisms + LAYER 2 interactions + LAYER 3 context_weights + hard_boundaries + signals + wiring, with the section banners preserved. Field derivation (all deterministic, registry-only, rule 8 — no invented knowledge): LAYER 1 per member — name = record name lowercased; fact = first sentence of mechanism_summary_for_context (drops instruction-voice tails); grade + source from evidence (basis + pinned_evidence DOIs); boundary = humanized evidence.caveats, plus 'contested by {citation}' ONLY when the dossier.dissent opens with a single citation-shaped clause (carries a year, one clause, <=160 chars) — prose-fronted dissents contribute no citation, so among the seed set only LA-01 yields one; active_when = precondition predicates with '== true' stripped, joined by OR; realizations = implementation ids with the '{ID}-' prefix removed; forbidden = hard_rule ids kebab-cased. LAYER 2 interactions = dedup relations where both endpoints are pack members: enables/enabled_by -> sequence-amplifying (ordered [enabler, enabled]), hybrid_with -> reinforcing, adjacent -> noted, orthogonality_note skipped; fact = relation note; grade = weaker of the two members' grades. LAYER 3 context_weights = per distinct precondition predicate, a strong entry and a negated inactive entry. hard_boundaries = union of members' hard_rules as status-facts ('{kebab-id}: forbidden — {rule}'), plus each member's boundary_test; rule text is voice-normalized (leading Do-not/Never stripped; any rule still carrying you/your/should/prefer collapses to bare 'forbidden') so the sheet stays instruction-free per its own 'no instructions' nature. signals.measured = sorted union of members' implementation metrics; tag/learning and the whole wiring block are constants from the reference. Two self-checks run before writing each file: it must re-parse as YAML, and no emitted prose outside comments may carry you/your/should/prefer. Stale pack-*.yaml files are removed on regenerate; pack-map.yaml and README.md are never touched. KNOWN DELTAS vs the hand reference (structure matches EXACTLY except one field; content is registry-derived, not the hand-condensed prose): (1) the reference put an optional 'boundary' on LAYER 2 interactions, but there is no registry field for an interaction-level boundary, so generated interactions omit it (adding one would be invented knowledge); (2) the two reference 'suppressing' interactions ([SC-06, high-pressure-framing], [all-four-saturated]) exist in no registry relation and do not appear — if wanted they must be added to the registry as relations first; (3) realizations use implementation-id stems, applies_to/version come from the pack map (so the generated paywall header shows the map's [paywall, pricing] and version 0.1.0, not the hand file's [paywall, checkout, pricing_element]/3.0.0). lib/types.ts gains PackMechanism, PackInteraction(Type), PackContextWeight, PackSignals, PackWiring, PackDatasheet (rule 13). package.json gains the 'packs' script. Renders nowhere yet by design — DATA ONLY. npm run validate and npm run build are green.",
      "area": "data"
    },
    {
      "id": "D-050",
      "date": "2026-07-16",
      "title": "Sufficiency scored per pack×segment on 5 computable criteria; general-only evidence flags a segment gap",
      "body": "The analyzer layer arrives: tools/analyzer.ts (npm run analyze) computes, for every pack-map element (D-048) × every ACTIVE segment (D-047), whether the knowledge is mature — the heart of this layer. Output is /analysis/sufficiency-matrix.json, a COMPUTED projection never hand-edited (render-cards/render-packs pattern, D-002/D-049); the ONE hand-authored input is /analysis/analyzer.config.yaml, holding everything owner-tunable: grade-letter weights (A=1, B=0.6, C=0.2; the +/- modifier collapses, A- → A), per-criterion status thresholds (default green≥0.8/amber≥0.5; dissent_completeness green=1.0/amber≥0.8 so one stripped dossier in a 3–4 mechanism pack turns the cell red, not amber), the segment → typical-funnel-stage map, segment-affinity boosts, and owner replication flags. Each cell carries {pack, segment, scores{5 criteria, each 0–1}, status red|amber|green, gaps:[criteria below green], segment_evidence}. The five computable criteria: (1) dissent_completeness — share of the pack's mechanisms whose dossier dissent is non-empty (missing dossier or blank dissent = 0), deliberately UNWEIGHTED: dissent is a knowledge-hygiene bar, and a stripped dossier fails it for every segment regardless of how relevant its mechanism is to that segment; (2) grade_sufficiency — weighted mean of grade-letter weights over the pack's mechanisms; (3) interaction_coverage — share of the pack's C(n,2) mechanism pairs connected by a registry relation in either direction (the same pairing logic render-packs projects into LAYER 2; orthogonality_note is excluded — it documents separation, not interaction); (4) context_coverage — share of the segment's typical funnel stages on which the pack holds ≥1 mechanism at grade ≥ min_context_grade (B) whose applicability lists the stage and does not exclude it; (5) freshness — 1 minus the weighted share of pack mechanisms in config replication_flags (empty today, so freshness defaults green; replication concerns currently live as prose in dossier dissent / evidence caveats, and flagging one is an explicit owner judgment, never derived from prose). Cell status = worst criterion status. SEGMENT RELEVANCE: a mechanism's fit to a segment is derived from its applicability × the segment-affinity config — weight = min(1, base × boost) where base is 1 when the mechanism's funnel_stages intersect the segment's typical stages (0.5 otherwise) and boost comes from segment_affinity (e.g. daily-habit strengthens HA-04/VR-02; occasional-high-consideration strengthens SP-08/FR-11; transactional-ecommerce strengthens SC-06/LA-01). Weights only redistribute emphasis inside a cell — they never fabricate evidence. Where no segment-specific judgment touches the pack's mechanisms, the cell scores on general evidence and carries segment_evidence: general_only — THE signal that segment-specific harvesting is needed (the Maria case: works for commerce, thin for others). IMPORTANT framing (rule 8): the config's segment_stages and segment_affinity seeds are PRODUCT JUDGMENT defaults, not scientific claims — flagged owner-reviewable in the file header. The analyzer fails loudly on config problems (an active segment missing from segment_stages, unknown stage/mechanism ids, malformed thresholds) rather than guessing. Constitution note: /analysis joins the repo as an analyzer surface — analyzer.config.yaml is hand-authored git-only data; sufficiency-matrix.json is generated output (the /cards precedent). lib/types.ts gains SufficiencyCriterion, SufficiencyScores, SufficiencyStatus, SegmentEvidence, SufficiencyThreshold, GradeLetter, AnalyzerConfig, SufficiencyCell, SufficiencyMatrix (rule 13). package.json gains the analyze script. Renders nowhere yet by design — DATA ONLY. Verified: 11 packs × 15 segments = 165 cells; one cell hand-checked against the underlying records; cells without an affinity entry carry general_only; a deliberately dissent-stripped mechanism turns its cells red on dissent_completeness. npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-051",
      "date": "2026-07-16",
      "title": "Gap planner ranks red/amber cells into a budget-bounded queue of targeted, segment-qualified harvests",
      "body": "The prioritization layer arrives: tools/gap-planner.ts (npm run gaps) turns the red/amber cells of analysis/sufficiency-matrix.json (D-050) into a RANKED, BUDGETED queue of targeted evidence harvests at analysis/research-queue.json — the discipline of 'research the biggest gaps in the segments that matter', not 'research everything'. The queue is a COMPUTED projection, never hand-edited (the render-cards/analyzer precedent, D-002/D-050); the ONE hand-authored input is a new gap_planner block appended to /analysis/analyzer.config.yaml (kept in the single analyzer config since it too is owner-tunable product judgment, git-only, no UI write surface). The block holds: segment_weights (segment id → importance weight, default 1.0 when omitted — PRODUCT-PRIORITY judgment, not science), segment_qualifiers (segment id → generic vocabulary tokens appended to a mechanism's own evidence terms, e.g. 'b2b enterprise', 'habit daily app'), and budget { max_tasks, estimated_calls_per_task, monthly_budget_share }. RANKING: importance = segment_weight × gap_size, where gap_size = Σ over the cell's failed criteria of (green_threshold − score) read from the same thresholds the analyzer uses — a severity measure, so a deep red cell in a high-weight segment (e.g. b2b 1.5) rises above a shallow amber. Ties break red-before-amber, then general_only-before-segment_specific (D-050's segment-gap flag), then weaker-mechanism-first. SATURATION: only cells with status red|amber are queued; a green cell is saturated and never appears — verified absent in the output. EXPANSION: the matrix is pack × segment but a harvest targets a mechanism, so each cell expands via /packs/pack-map.yaml (D-048) to its pack's registry mechanisms; a task addresses one (mechanism, segment) pair, deduped across packs (highest-importance cell wins) so an identical segment-qualified harvest is queued once. Each task = { gap_cell{pack,segment,status,gaps,segment_evidence}, mechanism, segment, importance, suggested_evidence_terms, reason }. suggested_evidence_terms are the mechanism's OWN evidence_terms (registry, D-015; fallback its name), capped at the first 3 and each appended with the segment qualifier — no invented science (rule 8), and they drop straight onto the evidence connector's terms= override (npm run connector -- evidence mechanism=... terms=...), i.e. the SAME harvest machinery, segment-qualified. reason is computed from the cell's failed criteria and their scores vs thresholds, never hardcoded. BUDGET (does NOT bypass the monthly cap system): N = min(gap_planner.budget.max_tasks, floor(remaining_monthly_calls × monthly_budget_share / estimated_calls_per_task)); remaining_monthly_calls comes from lib/ops.ts computeBudgetSnapshot (the SAME month-to-date snapshot the /ops gate and progress bar read), and estimated_calls_per_task mirrors the evidence connector's max_calls_per_run (150) so one task ≈ one run. The planner never triggers a harvest and never raises a cap — each queued task is still gated per-run by evaluateRunAgainstOps at harvest time (D-025), so per-run limits and the monthly budget remain the hard ceilings. The output carries a budget block recording month, remaining calls, the share, config vs budget max_tasks, and the applied N, plus candidate_count (red/amber mechanism × segment pairs before truncation) for transparency. The planner fails loudly (like the analyzer) on a missing matrix (points to npm run analyze), a missing gap_planner block, a malformed budget, an unknown segment in weights/qualifiers, or a segment that has gaps but no qualifier — it never harvests unqualified. lib/types.ts gains GapPlannerBudgetConfig, GapPlannerConfig, ResearchGapCell, ResearchTask, ResearchQueueBudget, ResearchQueue (rule 13). package.json gains the 'gaps' script. Renders nowhere yet by design — DATA ONLY; research-queue.json is generated output on the /analysis surface, not validated by tools/validate.ts (the sufficiency-matrix precedent). Decision policy encoded and logged by the tool: gaps prioritized by segment-weight × gap-size, budget-bounded; saturated cells excluded; addressing is targeted not exhaustive. npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-052",
      "date": "2026-07-17",
      "title": "Weekly maturation loop: analyze -> find gaps -> targeted harvest -> re-analyze -> regenerate packs; budget-bounded, self-limiting",
      "body": "The orchestration layer arrives: .github/workflows/maturation.yml — the weekly maturation loop, on a schedule (Mon 07:00 UTC, after the scheduled evidence run) and on workflow_dispatch (with an optional max_tasks input for small-N verification runs). The loop: analyze -> find gaps -> targeted harvest -> re-analyze -> regenerate packs; budget-bounded, self-limiting. Steps: (1) npm run analyze recomputes the full sufficiency matrix (D-050) and the pre-harvest matrix is kept as the diff baseline; (2) npm run gaps recomputes the ranked, budget-bounded research queue (D-051); (3) a new ops-gate mode, plan-queue, gates the queue's top-N tasks through the EXISTING guards — paused, health-down (D-030), per-run limits and the monthly budget via evaluateRunAgainstOps (D-025) — never re-implementing them; evaluateRunAgainstOps gains an optional pendingSpend argument so the batch's already-accepted estimates are projected cumulatively on top of month-to-date spend (pendingSpend only tightens the gate, never loosens it): accepting task k accounts for tasks 1..k-1, so the loop can NEVER exhaust the budget and raise_cap is never passed. A task that no longer fits gets action 'defer' with a logged plain-language reason — it stays in the queue and surfaces again next week (deferral is a first-class plan outcome, recorded in the job summary, not a silent drop). Cadence and registry freshness deliberately do NOT gate queue tasks: the queue is gap-driven (which knowledge is thin), not change-driven (what the owner touched) — those guards keep governing the scheduled _ops-target fleet in connectors.yml. Each due task is harvested by the SAME evidence connector with the task's segment-qualified terms= override (suggested_evidence_terms, D-051), retry-once-after-backoff and the deduplicated ops-issue contract (D-022) reused verbatim; the workflow shares the exact 'connectors-s2' concurrency group so it can never call Semantic Scholar concurrently with connectors.yml/harvest.yml (D-027). (4) The analyzer gains a scoped re-score — npm run analyze -- packs=a,b re-scores ONLY the listed packs' cells and preserves every other cell verbatim from the existing matrix (pack-map order kept; fails loudly if the grid changed) — so the loop re-analyzes exactly the cells its harvests addressed. (5) render-packs gains the same packs= filter — regenerating ONLY the packs whose cells flipped to green (stale-pack removal is skipped on a scoped run, which cannot tell stale from not-rendered). (6) Changes to analysis/, corpora/ and packs/ are committed as 'maturation: week {date} — filled {N} gaps' (a dedicated maturation: commit prefix for the loop's auto-commits, extending the D-016 conventional-commit set), and the job summary carries the week's red->green cell diff (computed by comparing the baseline and re-scored matrices), the regenerated packs, the run's spend (budget snapshot delta), and the deferred-task count. Honest expectation: a harvest alone fills corpora — cell scores move only when the owner upgrades registry grades/dossiers (rule 8), so red->green flips are expected after owner review lands, and a week with zero flips is a truthful, common outcome. Nothing here touches the Ventora app surface; the loop runs entirely in Actions on the repo's file-based data.",
      "area": "operations"
    },
    {
      "id": "D-053",
      "date": "2026-07-17",
      "title": "Maturation cockpit visualizes knowledge sufficiency; the growing matrix is the proof the core is alive",
      "body": "The maturation layer (D-050/D-051/D-052) finally RENDERS: /maturation is the Control Center cockpit that makes the maturing knowledge visible — matrix, gaps, weekly progress, honest, read from files. Until now analysis/*.json rendered nowhere by design; this step consumes it in the app for the first time (a narrow read extension, no new write surface). The page (app/maturation/page.tsx, a server component, no client JS) shows four things, each with a file-provenance footer and a designed empty state: (1) the sufficiency matrix as a heatmap — packs (rows, pack-map.yaml order, D-048) × active segments (columns, grouped by axis from segments.yaml, D-047), cell color = computed status via lib/status.ts CELL_STATUS_META, a hollow-ring marker for segment_evidence general_only, and a CSS-only hover tooltip carrying the 5 scores, the gaps, and the segment-evidence flag; a segment active in segments.yaml but absent from the matrix renders as the neutral 'not analyzed' state (CELL_NOT_ANALYZED_META), honest, never red; (2) this week's research queue (analysis/research-queue.json) — the budget line plus each task's mechanism × pack × segment, importance, reason, and suggested terms; (3) a maturation log (analysis/maturation-log.json) — the weekly red->green cell diff, packs regenerated, and spend; (4) a coverage summary — % green overall, per pack, per segment. HONESTY RULE (.cursorrules #2): every cell, count, and log entry is READ from analysis/*.json + segments.yaml and COMPUTED at render time in lib/maturation.ts / lib/status.ts; the 'red'|'amber'|'green' and 'general_only'|'segment_specific' literals live ONLY in lib/status.ts (CELL_STATUS_META, CELL_STATUS_ORDER, CELL_NOT_ANALYZED_META, SEGMENT_EVIDENCE_META) — app/ carries no status literal, so the grep check stays clean. A 'segments are evolving' note computes the active/retired counts and provenance breakdown from segments.yaml (never hardcoded — the axis grows and the note grows with it). The Overview ROOMS nav gains a /maturation link. PERSISTENCE: the weekly loop's story used to live only in the ephemeral GitHub Actions job summary; a new tool tools/maturation-log.ts (npm run maturation-log) records it into analysis/maturation-log.json — generated output, never hand-edited (the sufficiency-matrix / research-queue precedent, D-050/D-051), computed only from the artifacts the loop already produces (cell-diff.json, budget-before/after.json, queue-plan.json); packs_regenerated mirrors the workflow's flipped_packs (packs with a cell that flipped to green) and a zero-flip week is recorded truthfully. maturation.yml (D-052) gains one step after the summary and before the commit; the existing 'git add analysis' carries the log file into the maturation: commit. lib/types.ts gains MaturationCellChange, MaturationLogEntry, MaturationLog (rule 13); the yaml package moves from devDependencies to dependencies since the app now parses segments.yaml / pack-map.yaml at runtime (not speculative — needed now by lib/maturation.ts). maturation-log.json is generated output on the /analysis surface, not validated by tools/validate.ts (the matrix/queue precedent). Verified: the heatmap matches sufficiency-matrix.json (85 red / 70 amber / 10 green across 165 cells); changing a cell's score/status in the matrix JSON moves the cell and the coverage numbers; a segment added to segments.yaml but absent from the matrix renders neutral, not red; the empty log renders its honest empty state; every displayed number traces to a file. npm run validate and npm run build green.",
      "area": "architecture"
    },
    {
      "id": "D-054",
      "date": "2026-07-17",
      "title": "Segments evolve — owner adds now, analyzer-suggestion designed for later; new segments enter the matrix red and mature via the loop",
      "body": "The product-segment axis (segments/segments.yaml, D-047) is a SEED, NOT A FIXED SET — segments grow, and the growth feeds the same maturation loop the mechanisms use. Two paths, one available now and one designed for later. PATH A (available now): the owner adds a segment with a single git edit — one entry with provenance 'owner' (the axis stays git-only, no UI write surface, .cursorrules #1). On the next analyzer run it enters the sufficiency matrix AUTOMATICALLY as an honest ALL-RED column: tools/analyzer.ts no longer fails loudly when an active segment has no analyzer.config.yaml segment_stages entry — instead that segment is a BOOTSTRAP segment and every pack × bootstrap-segment cell scores 0 on all five criteria, status red, all criteria as gaps, segment_evidence general_only. This is the honest state (.cursorrules #2): with no stage map and zero segment-specific evidence, nothing has been demonstrated FOR the segment yet, so red is the truth — never a hardcoded status, always computed from the absence of data. A malformed segment_stages entry (present but with an unknown stage) is STILL a loud error; only a fully-absent entry means bootstrap. Because a bootstrap cell has the maximal gap_size, tools/gap-planner.ts (D-051) ranks it to the TOP of the research queue; the planner derives the segment's harvest qualifier mechanically from its own id (hyphens → spaces, e.g. 'ai-agent-tools' → 'ai agent tools') so the new segment gets targeted, segment-qualified tasks before the owner hand-tunes anything — product vocabulary from the owner's own slug, never invented science (rule 8). A CONFIGURED segment (one with a segment_stages entry) that is missing a gap_planner.segment_qualifiers entry still FAILS loudly — the fallback applies only to brand-new unconfigured segments, so a real typo is never silently swallowed. The owner graduates a segment out of bootstrap by adding segment_stages (required to score on the five criteria) and optionally segment_affinity / gap_planner qualifiers+weights to analyzer.config.yaml. PATH B (designed, NOT scheduled): tools/segment-suggest.ts (npm run segment-suggest) is the discovery analog for segments — the counterpart of the mechanism seed stubs (D-046) — and is deliberately INERT: it reads nothing, writes nothing, and is in NO workflow and on NO schedule; running it prints 'designed, not scheduled — no candidates written'. Its committed header specifies the future algorithm: read the harvested corpora (corpora/evidence titles/abstracts/terms, corpora/benchmarks industry categories, corpora/wayback domains) for recurring product-context clusters, SUBTRACT everything already covered by an active segment (id, definition vocabulary, segment_qualifiers), and propose the uncovered clusters as SegmentCandidate records appended to segments/candidates.json for OWNER APPROVAL. On approval the owner hand-adds the segment to segments.yaml with provenance 'analyzer' (a tool never edits the axis, never invents science) — after which it enters the matrix all-red via Path A. segments/candidates.json { version, generated_at, candidates[] } (each { id, group, definition_draft, evidence_note, proposed_at, status: proposed|approved|rejected }) is seeded as an empty queue and validated by tools/validate.ts: schema pinned to the reader contract lib/types.ts SegmentCandidate via a satisfies constraint (same drift-guard as D-047), unique candidate ids, and no candidate id may collide with an existing segment (a candidate proposes a NEW segment). lib/types.ts gains SegmentCandidateStatus, SegmentCandidate, SegmentCandidateQueue (rule 13). package.json gains the segment-suggest script (discoverability only). The /maturation cockpit's 'Segments are evolving' panel now shows the candidate count read from candidates.json with an honest empty state ('0 candidates — segment-suggest is designed but not scheduled') — never hardcoded (lib/maturation.ts computeSegmentCandidates). Documented in docs/connectors-runbook.md (new 'Segment evolution' section: how the owner adds a segment, the all-red bootstrap, graduating to configured, and the segment-suggest design status) and segments/README.md. Verified: adding a test segment with provenance owner and no config entries makes npm run validate stay green, npm run analyze emit its pack cells all-red, and npm run gaps queue research tasks for it with the id-derived qualifier; removing it restores the committed matrix/queue; npm run segment-suggest writes nothing and is in no workflow. npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-055",
      "date": "2026-07-17",
      "title": "Gaps are typed by their filler — harvest vs structural; the loop must never dispatch a harvest against a structural gap",
      "body": "Every failed criterion in a sufficiency-matrix cell (analysis/sufficiency-matrix.json, D-050) is now LABELED by what can actually close it — the fix that stops the wheel spinning. tools/analyzer.ts emits a new typed_gaps array on every SufficiencyCell: one entry per failing criterion { criterion, value (the cell's score), threshold (the green bar it falls short of), fix_type, what_would_close_it }. fix_type is one of two fillers: HARVEST gaps close by fetching more/better evidence through the connector (grade_sufficiency; freshness); STRUCTURAL gaps close only by owner edits in git and NO harvest can touch them (interaction_coverage — registry relations between the pack's mechanisms; context_coverage — pack composition / a grade≥B mechanism covering the segment's funnel stages). dissent_completeness is STRUCTURAL: dossier dissent is owner-authored (rule 8), so no fetch fills it (it only surfaces as a gap in bootstrap all-red cells today). The general_only condition is ALSO surfaced as a harvest gap: when a non-green cell rests on general evidence only, typed_gaps carries a pseudo-criterion 'segment_evidence' entry (value 0, threshold 1, fix_type harvest) whose closer is segment-qualified evidence plus a segment_affinity entry — segment_evidence is not one of the five scored criteria but it IS harvest-closable, so it belongs in the typing. A cell's gaps can be MIXED type (a red cell is commonly grade_sufficiency=harvest + interaction_coverage/context_coverage=structural + segment_evidence=harvest). The reason the loop must never dispatch a harvest against a structural gap: fetching papers can never add a registry relation or recompose a pack, so a harvest aimed at a structural gap spins forever without moving the score. The existing gaps: SufficiencyCriterion[] field STAYS unchanged — tools/gap-planner.ts (gapSize, reasonFor, ResearchGapCell.gaps, D-051) and the /maturation cockpit tooltip (app/maturation/page.tsx, cell.gaps.includes) still read it; typed_gaps is the richer superset the maturation loop reads to route work. lib/types.ts gains GapFixType and TypedGap and SufficiencyCell.typed_gaps (rule 13); the constants FIX_TYPE and WHAT_WOULD_CLOSE_IT live in tools/analyzer.ts. A scoped re-score (npm run analyze -- packs=..., D-052) fails loudly if the existing matrix predates gap typing (any preserved cell missing typed_gaps), so a scoped run can never emit a partially-typed matrix; MATRIX_VERSION bumped 0.1.0 → 0.2.0. Verified: every red cell shows interaction_coverage/context_coverage as structural and grade_sufficiency/segment_evidence as harvest, and no gap is untyped; npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-056",
      "date": "2026-07-17",
      "title": "Loop routes by fix_type; structural gaps go to an authoring queue, not the API",
      "body": "The weekly maturation loop now ROUTES gaps by their fix_type (D-055) instead of harvesting every red/amber cell. Only HARVEST gaps generate connector tasks and consume budget; STRUCTURAL gaps are written to a new owner-facing analysis/authoring-queue.json and NEVER touch the API. The reason: fetching papers can never add a registry relation, recompose a pack, or author dossier dissent, so dispatching connectors against interaction_coverage / context_coverage / dissent_completeness gaps burns Semantic Scholar / OpenAlex budget for zero possible cell flips — the wheel spins forever. THE ROUTING RULE (tools/gap-planner.ts): a cell is HARVESTABLE iff it has ≥1 SCORED harvest gap (grade_sufficiency or freshness in cell.gaps, read via typed_gaps fix_type). The segment_evidence harvest flag alone does NOT make a cell harvestable — it is not a scored criterion, and if every scored gap is structural no segment-qualified evidence can flip the cell, so harvesting it is pure budget burn (this is exactly what drops the interaction-heavy red cells out of the harvest queue). A MIXED cell routes to BOTH queues: its harvest gaps become connector tasks, its structural gaps become an authoring task. HARVEST PATH: only harvestable cells expand to (mechanism, segment) candidates; importance = segment_weight × gap_size sized over the cell's HARVEST gaps ONLY (not all failed criteria), so a mixed cell ranks by what a harvest can actually fix; reason cites only the harvest gaps. Budget logic (computeBudgetSnapshot, effective_max_tasks) is untouched — it simply sees fewer candidates, so spend drops. Segments needing a harvest qualifier are now the segments of HARVESTABLE cells only, so a structural-only segment no longer fails loudly demanding a qualifier it will never use. STRUCTURAL PATH: every red/amber cell with ≥1 structural typed_gap becomes an AuthoringTask { pack, segment, status, importance = segment_weight × Σ structural (threshold − value), segment_evidence, structural_gaps[] }, sorted importance-desc (red before amber, stable by pack/segment), written to analysis/authoring-queue.json { version, generated_at, config_version, matrix_generated_at, cell_count, tasks } — a COMPUTED projection never hand-edited (the research-queue precedent, D-051), always written even when empty (honest empty state). To make the authoring queue ACTIONABLE, tools/analyzer.ts now enriches the two structural coverage gaps with owner-facing detail: interaction_coverage carries missing_interaction_pairs (the pack's unlinked member id pairs, locale-stable) and context_coverage carries uncovered_stages (the segment funnel stages with no grade≥min_context_grade mechanism) — so the owner sees exactly which relations to author / which stages to cover, per pack×segment, ranked by importance. WORKFLOW (.github/workflows/maturation.yml): the loop's harvest/gate steps need no change (with the planner filtered, purely-structural cells never reach queue-plan.json), and the existing 'git add analysis' carries the new file into the maturation: commit; the job summary now reports BOTH — 'N harvest task(s) dispatched, M structural task(s) queued for authoring'. tools/maturation-log.ts records structural_queued (count of authoring-queue tasks) in the weekly entry so the /maturation cockpit history has it. lib/types.ts gains AuthoringTask, AuthoringQueue, TypedGap.missing_interaction_pairs / uncovered_stages, and MaturationLogEntry.structural_queued (optional, back-compatible); authoring-queue.json is generated output on the /analysis surface, not validated by tools/validate.ts (the matrix/queue precedent). Verified: re-running the loop on the current matrix dispatches far fewer harvests for the interaction-heavy red cells and populates the authoring queue instead; the research-queue budget block claims fewer tasks (spend drops accordingly); npm run validate and npm run build green.",
      "area": "operations"
    },
    {
      "id": "D-057",
      "date": "2026-07-18",
      "title": "Interactions are first-class authored records; interaction_coverage reads them; this is the primary structural filler",
      "body": "interaction_coverage is the biggest structural gap in the sufficiency matrix (D-050), and until now the ONLY way to fill it was a registry relation on a mechanism record — a thin, single-note projection. This introduces a first-class OWNER-AUTHORED interaction record type and store: /interactions/{MECH-A}__{MECH-B}.json = { pair (sorted by id.localeCompare, matching the filename), type (sequence-amplifying | reinforcing | suppressing | neutral), fact, grade, boundary, source }, schema at /interactions/interaction.schema.json (mechanism/dossier schema-file pattern). Content is owner-provided, never Cursor-generated (rule 8): the store ships with the schema file only, no example records. lib/types.ts gains InteractionType and InteractionRecord (rule 13). READS (three): (1) tools/analyzer.ts — interactionCoverage now counts a pair as covered if a non-orthogonality_note relation OR an authored record connects it, and authored pairs drop out of missing_interaction_pairs, so the typed structural gap and the authoring queue shrink automatically as records land (gap-planner unchanged). DECISION: ALL FOUR types count as covered, INCLUDING neutral — an authored record means the owner examined the pair, which is exactly what coverage measures; this differs from the cheap orthogonality_note RELATION, which stays excluded because it is a by-product of writing a mechanism, not a deliberate pairwise judgment. (2) tools/render-packs.ts LAYER 2 — already the pack's interaction layer; it now MERGES the store with relations: an authored record maps directly (combination = pair, type/fact/grade/boundary/source from the record, grade owner-stated not derived) and REPLACES the relation-derived entry for the same pair (richer, owner-curated); relations still fill pairs with no record. PackInteractionType gains suppressing/neutral; PackInteraction gains optional boundary/source (authored entries only). With an empty store LAYER 2 output is byte-identical to before. (3) tools/validate.ts — a new pass compiles the schema and validates every /interactions/*.json plus cross-checks: pair sorted and distinct, both ids in the registry roster, filename = {pair[0]}__{pair[1]}.json, no pair authored twice. AUTHORING VIEW: /maturation gains an 'Interaction authoring' panel (lib/maturation.ts computeInteractionAuthoring) that aggregates the authoring-queue's interaction_coverage missing_interaction_pairs into a ranked, deduped list, each pair showing a computed 'not yet authored' vs 'authored' chip (from file existence, mapped through lib/status.ts INTERACTION_AUTHORING_META — no status literal in app/), the expected filename to create in git, and the affected pack×segment cells. Read-only: knowledge is edited in git, no UI write surface. This is the PRIMARY structural filler for the matrix. VERIFY: adding one owner-provided interaction JSON raises interaction_coverage on the affected cells (npm run analyze), removes the pair from the authoring queue (npm run gaps), and, when both members share a pack, injects it into that pack's LAYER 2 (npm run packs). npm run validate and npm run build green with the empty store.",
      "area": "data"
    },
    {
      "id": "D-058",
      "date": "2026-07-19",
      "title": "Harvest optimizes for diversity and novelty, not volume; re-fetching canon is not progress",
      "body": "When harvesting IS the right fix (a scored harvest gap, D-056), a harvest must broaden the corpus, not re-read it. The evidence connector's segment-qualified harvest (tools/connectors/evidence.ts, v2.3.0 → 2.4.0) is upgraded on four axes. (1) QUERY-TERM VARIATION: each term now fans out across viewpoints instead of one canon query — 'canon' (the term as-is: OpenAlex cited_by_count:desc 25 + Semantic Scholar relevance 15, unchanged), 'recent' (OpenAlex publication_date:desc, 10), and five CONTRAST ANGLES (application 'applied intervention', critique 'critique criticism', replication 'replication', boundary 'boundary conditions moderators', cross-domain 'cross-domain generalization', 10 each). The angle suffixes are generic ACADEMIC VOCABULARY — a documented constant (CONTRAST_ANGLES), the same pattern as DISSENT_MARKERS — never invented science (rule 8); they steer which slice of the literature a query surfaces, not what the evidence says. (2) SOURCE SPREAD: the contrast angles ALTERNATE OpenAlex / Semantic Scholar by index (even → OpenAlex, odd → S2) so a run draws across both wired APIs, not one; canon already uses both. (3) DEDUP-AWARE NOVELTY: after the existing DOI/OpenAlex-id/title dedupe, the deduped SEARCH records (owner pins and snowballed references excluded — they are structural completeness, not query diversity) are checked against the PREVIOUS corpus file; a harvest whose unique results are more than LOW_NOVELTY_KNOWN_SHARE (0.8) already-in-corpus sets low_novelty: true and emits RunResult.warnings.low_novelty (same manifest channel as s2_throttled). The corpus file is still written (it is honest data; the anti-regression guardrail D-038 is unchanged), but the flag prevents the loop from thinking it filled a gap by re-fetching. A first harvest (no prior file) is novelty_rate 1, never low. (4) RECENCY BALANCE: the dedicated 'recent' query keeps newer work in reach so segments tied to newer product forms aren't starved by citation sort. Every harvest writes a diversity_report into corpora/evidence/{id}.json { viewpoint_spread[] (per angle: queries, returned, unique deduped records surfaced), source_spread[] (per API, same), recent_records, recency_rate, novelty { previous_corpus_records, unique_records, already_in_corpus, new_records, novelty_rate, low_novelty, known_share_threshold } }. Attribution is exact: dedupeRecords now returns the raw clusters aligned to the merged records, and mergeCluster unions each cluster's search_angles, so a unique record is credited to every angle/API that surfaced any variant of it. quote() (D-025) is updated deterministically to the new per-term call/record math (canon 2 + recent 1 + 5 contrast angles, alternating APIs) so the /ops dry-run gate stays honest; ~8 search calls/term (3 terms ≈ 24) stays well under the per-run budget and the S2 1 rps queue (D-027) — only the canon S2 query is throttleable, contrast S2 queries keep their fixed batch. LOOP: tools/gap-planner.ts (npm run gaps) now SKIPS a candidate whose mechanism's last harvest used the SAME terms and came back low_novelty — re-fetching the same canon is pure budget burn — BEFORE it consumes a budget slot; the skip is COUNTED in research-queue.json low_novelty_skipped (never silently dropped: change the mechanism's terms or the segment qualifier and it re-enters). .github/workflows/maturation.yml (D-052) counts how many of a run's harvested mechanisms came back low_novelty (jq over their corpus files' diversity_report) into a job-summary line and passes it to tools/maturation-log.ts --low-novelty, recorded as MaturationLogEntry.low_novelty_harvests (optional, back-compatible) and shown on the /maturation cockpit weekly log. VALIDATION: tools/validate.ts gains a pass — every corpora/evidence/*.json (incl. *.regression.json) that carries a diversity_report is validated against an inline schema (viewpoint + source spread, novelty block with boolean low_novelty); a file without the block is a pre-D-058 corpus and stays valid (additive, never a new hard requirement on old data). lib/types.ts gains ResearchQueue.low_novelty_skipped and MaturationLogEntry.low_novelty_harvests (rule 13). THE POLICY: harvest optimizes for diversity and novelty, not volume; re-fetching canon is not progress. Verify: a first diversified harvest of a genuine gap writes a diversity_report with non-empty viewpoint_spread and both APIs in source_spread; an immediate re-run with the same terms collapses novelty_rate and sets low_novelty (report + manifest warning); npm run gaps then skips that candidate and counts it in low_novelty_skipped. npm run validate and npm run build green.",
      "area": "operations"
    },
    {
      "id": "D-059",
      "date": "2026-07-19",
      "title": "Evidence exhaustion is detected and surfaced honestly; the loop never harvests indefinitely against thin literature",
      "body": "Some segment×mechanism gaps have thin REAL literature — no amount of good harvesting closes them, and the D-058 low-novelty skip only stops re-fetching the SAME terms, so a thin gap can still churn forever as segment qualifiers vary and the per-mechanism diversity_report is overwritten by other segments' harvests. This adds persistent per-gap memory and an honest give-up: the loop concludes 'the literature is thin' instead of harvesting the same canon week after week. THE LEDGER: a new tool tools/harvest-history.ts (npm run harvest-history) appends one attempt per harvested (mechanism × segment) target per weekly run to analysis/harvest-history.json { version, generated_at, entries: { 'MECH|segment': { attempts:[{week, terms, low_novelty}], low_novelty_streak, streak_since } } } — a COMPUTED projection, never hand-edited (the maturation-log precedent, D-053), recorded ONLY from artifacts the loop already produces (queue-plan.json's action:'run' entries + each corpus's diversity_report.novelty.low_novelty, D-058); it re-computes nothing. Re-running a week REPLACES that week's attempt (idempotent), then recomputes each target's trailing low-novelty streak (consecutive low-novelty weeks; a novel harvest resets it). Only tasks the plan RAN add an attempt — a deferred/skipped task did not harvest. THE RULE (analysis/analyzer.config.yaml gains exhaustion.low_novelty_attempts: K, default 3): tools/analyzer.ts marks a cell evidence_exhausted:true when it still has a SCORED harvest gap below threshold (grade_sufficiency/freshness — mirrors the D-056 harvestable test; the segment_evidence pseudo-gap alone never qualifies) AND every one of its pack's mechanisms has low_novelty_streak ≥ K for that segment in the ledger. The cell keeps its computed scores/status (exhaustion is an honest 'best available', not a status flip) and records exhaustion { attempts (total low-novelty harvests across the pack's mechanisms), weeks (min shared low-novelty streak, ≥K), since (the week from which every mechanism has been continuously low-novelty), best_scores (the cell's current harvest-criterion scores) }. Reversible: a novel harvest resets a mechanism's streak, so the next analyze drops the flag and the cell re-enters. Absent ledger or absent exhaustion block → exhaustion never fires (fresh-repo behavior). THE QUEUE (tools/gap-planner.ts): an evidence_exhausted cell is removed from the harvest path BEFORE qualifier requirements and budget sizing — no more harvesting against thin literature — and the removed (mechanism × segment) candidates (only those not still reachable via a non-exhausted cell) are counted in research-queue.json evidence_exhausted_skipped (mirror of low_novelty_skipped, never silently dropped). Each exhausted cell is instead routed to analysis/authoring-queue.json as an AuthoringTask with alternative_fill:true, fill_options:[owner_judgment, cross_domain_analogy, accept_lower_confidence], and its exhaustion summary — owner judgment is the only remaining closer, exactly what the authoring queue is for; it is added ALONGSIDE any structural gaps the cell also has, ranked by its (now unharvestable) harvest gap-size when it has no structural gap. THE WORKFLOW (.github/workflows/maturation.yml): a new step runs harvest-history.ts BEFORE the re-analyze step, so a cell crossing K THIS week is marked exhausted by the very next scoped re-analyze (same run, not a week later); the re-analyze step counts evidence_exhausted cells in the re-scored matrix into the job summary and passes it to tools/maturation-log.ts --evidence-exhausted, recorded as MaturationLogEntry.evidence_exhausted (optional, back-compatible). The existing 'git add analysis' carries harvest-history.json into the maturation: commit. THE COCKPIT (/maturation): exhausted cells render in the muted plan-slate token CELL_EXHAUSTED_META ('thin literature — best available', #7C93A8 — the existing plan-blue, no new palette) rather than the red alert; coverage counts them in a distinct 'exhausted' band (COVERAGE_BAND_ORDER) apart from red so a proven-thin gap never shows as an actionable red-forever cell; a new 'Thin literature — best available' panel lists each exhausted cell's best-achievable scores, harvest effort, and the three alternative fillers (computeThinLiterature), with an honest empty state. All status/label literals stay in lib/status.ts (CELL_EXHAUSTED_META, COVERAGE_BAND_META, ALTERNATIVE_FILL_META); app/ carries no literal (honesty rule, grep-clean). lib/types.ts gains ExhaustionConfig, CellExhaustion, SufficiencyCell.evidence_exhausted/exhaustion, ResearchQueue.evidence_exhausted_skipped, AlternativeFillOption, AuthoringTask.alternative_fill/fill_options/exhaustion, MaturationLogEntry.evidence_exhausted, and the HarvestAttempt/HarvestHistoryTarget/HarvestHistory trio (rule 13). package.json gains the harvest-history script. harvest-history.json is generated output on the /analysis surface, not validated by tools/validate.ts (the sufficiency-matrix / research-queue / maturation-log precedent, D-050/D-051/D-053). THE POLICY, logged by the planner: evidence exhaustion is detected and surfaced honestly; the loop never harvests indefinitely against thin literature. Verify: a deliberately un-fillable gap (obscure segment×mechanism) fed 3 weekly low-novelty attempts per mechanism gets marked evidence_exhausted after the 3rd, leaves the research queue (counted in evidence_exhausted_skipped), appears in the authoring queue with alternative_fill, and renders on the cockpit as 'thin literature — best available' rather than red; a single novel harvest clears the streak and it re-enters. npm run validate and npm run build green.",
      "area": "operations"
    },
    {
      "id": "D-060",
      "date": "2026-07-19",
      "title": "Sufficiency thresholds are stage-aware and explicit; green at seed stage means seed-adequate, not final",
      "body": "An 80% interaction_coverage bar is UNREACHABLE at t=0 with 12 seed mechanisms, so a stage-blind 'mature' threshold paints honest early progress red — the red then encodes the project's STAGE, not a real knowledge gap. This makes sufficiency thresholds STAGE-AWARE in the analyzer config: a single owner-tunable knob maturity_stage (seed | growing | mature) selects which per-criterion thresholds are active. THE CONFIG (analysis/analyzer.config.yaml, version 0.1.0 → 0.2.0): the flat thresholds: block is replaced by maturity_stage: seed plus stage_thresholds: { seed, growing, mature }, each a { default } + per-criterion override map (the same shape the flat block had). seed sets default { green 0.5, amber 0.3 } and interaction_coverage { green 0.4, amber 0.2 }; growing default { green 0.65, amber 0.4 } / interaction_coverage { green 0.6, amber 0.35 }; mature default { green 0.8, amber 0.5 } — mature is the UNCHANGED final bar, so nothing about the eventual standard is lowered. dissent_completeness stays a hard hygiene bar { green 1.0, amber 0.8 } at EVERY stage: hygiene is not maturity, so one stripped dossier still reds the cell regardless of stage. THE GUARANTEE — NOT A SILENT LOWERING: tools/analyzer.ts validateConfig requires maturity_stage to be one of the three and every stage to carry a valid default (0 ≤ amber ≤ green ≤ 1), and enforces MONOTONICITY loudly — for every resolved criterion, green and amber must be NON-DECREASING seed → growing → mature; a stage that relaxed below an earlier one fails the build. The bar only ever rises. THE FLOW stays one-directional: config → analyzer → matrix header → cockpit, so no component reads a threshold that isn't in a generated file. The analyzer resolves the active stage once (activeThresholds) — thresholdFor reads it exactly as before — and STAMPS maturity_stage + the resolved thresholds map into the matrix header (analysis/sufficiency-matrix.json gains maturity_stage and thresholds alongside config_version). tools/gap-planner.ts reads the same maturity_stage + stage_thresholds to size gaps (greenThreshold), so gap distances shrink/grow consistently with what actually fails a cell at the active stage. THE COCKPIT (/maturation): a 'stage: {stage}' chip sits beside the % green chip in the header; the Sufficiency matrix panel renders a ThresholdStrip stating the active stage, its plain-language meaning ('green means seed-adequate, not final — the bar rises as the knowledge matures'), and the per-criterion green/amber bars — every value READ from matrix.maturity_stage / matrix.thresholds, nothing hardcoded (honesty rule, .cursorrules #2; grep-clean of threshold literals in app/). A matrix predating the stage-aware header renders no strip (honest absence, not a fabricated bar). lib/types.ts gains MaturityStage and StageThresholds, replaces AnalyzerConfig.thresholds with maturity_stage + stage_thresholds, and adds SufficiencyMatrix.maturity_stage + thresholds (rule 13). THE POLICY: thresholds are stage-aware and explicit; green at seed stage means seed-adequate, not final. Verify: npm run analyze at maturity_stage: seed vs mature visibly changes the green/amber/red counts and which cells are green; the cockpit shows the active stage chip + threshold strip with values matching the config; no threshold literal lives in app/ or lib/ components. npm run validate and npm run build green.",
      "area": "data"
    },
    {
      "id": "D-061",
      "date": "2026-07-19",
      "title": "Cockpit distinguishes gap types and filler routes; the owner sees what to harvest vs what to author",
      "body": "Red/green alone hid WHICH filler a gap needs — the owner could see a cell was failing but not whether it was waiting on an automated harvest or on their own edit in git. The /maturation cockpit now makes the fix_type routing (D-055/D-056) and the exhaustion signal (D-058/D-059) VISIBLE per cell and per queue, so the same split the loop acts on is the split the owner reads. This is a cockpit-visibility step only: no analyzer/planner/workflow change, and the honesty rule holds — every route, count, and flag is COMPUTED at render time from analysis/*.json + segments.yaml, and every new literal lives in lib/status.ts (app/ stays grep-clean). VOCABULARY (lib/status.ts): FIX_TYPE_META maps GapFixType → { harvest (amber, automated connector work), author (slate, owner edit in git) }; computeCellRoute(cell) mirrors the gap planner's routing (lib never imports tools/, so the policy is duplicated, not shared) into green | exhausted | harvest | authoring — green is saturated, evidence_exhausted routes to exhausted (D-059), a cell with ≥1 SCORED harvest gap (grade_sufficiency/freshness, the segment_evidence pseudo-gap excluded, D-056) routes to harvest, and a red/amber cell whose only gaps are structural routes to authoring. COVERAGE (lib/maturation.ts): StatusCounts gains an authoring band and tally() classifies each cell by computeCellRoute, so COVERAGE_BAND_ORDER is now red | amber | authoring | exhausted | green — the summary distinguishes green, evidence-exhausted best-available (D-059), awaiting-authoring (owner edits), and the actionable red/amber a harvest can still move; a structural or thin-literature gap no longer inflates the red-forever share. CELL HOVER (app/maturation/page.tsx CellTooltip): the flat 5-score list is replaced by two groups built from cell.typed_gaps — 'harvest gaps' (grade/freshness + the segment_evidence pseudo-gap) and 'authoring gaps' (interaction/context/dissent) — each line showing the criterion, its score vs threshold, and a route chip (FIX_TYPE_META); passing criteria stay listed with scores; below are honest flag lines for evidence_exhausted (existing copy), low novelty ('low novelty: N/M mechanisms on a repeat-harvest streak', from a new analysis/harvest-history.json reader, omitted when the ledger is absent), the segment-evidence flag, and the maturity stage. TWO QUEUES SIDE BY SIDE: the research queue is retitled 'Harvest queue — automated' (budget-bounded, connector-dispatched, content unchanged) and sits in a 2-column grid beside a new 'Authoring queue — manual' panel that reads analysis/authoring-queue.json directly — stats (cells queued, thin-lit alternative-fill count), the ranked tasks (pack × segment, status chip, importance, per-structural-gap criterion chips with route, a thin-literature marker + alternative fillers for exhausted cells) capped at 10 with an honest '+ N more in analysis/authoring-queue.json' overflow line, and a designed empty state. The existing 'Thin literature' and 'Interaction authoring' panels stay below as detail views. lib/status.ts gains FIX_TYPE_META, CellRoute + computeCellRoute, COVERAGE_AUTHORING_META and the extended COVERAGE_BAND_ORDER; lib/maturation.ts gains StatusCounts.authoring, MATURATION_PATHS.harvestHistory + loadHarvestHistory, and CellNovelty + computeCellNovelty. THE POLICY: cockpit distinguishes gap types and filler routes; the owner sees what to harvest vs what to author. Verify: hovering a current red cell shows interaction_coverage/context_coverage under 'authoring gaps' and segment_evidence under 'harvest gaps'; the coverage bar shows the current structural-only reds as 'awaiting authoring' (harvest queue honestly empty, authoring queue populated with the ranked structural tasks); no status literal lives in app/. npm run validate and npm run build green.",
      "area": "architecture"
    },
    {
      "id": "D-062",
      "date": "2026-07-19",
      "title": "S7 Perception & comprehension added as a cross-cutting L0 taxonomy node",
      "body": "S7 added as a cross-cutting L0 node — perception precedes motivation; it applies to every generated element, not to specific funnel stages. The owner-provided node (registry/taxonomy.json) is { id: S7, name: 'Perception & comprehension', anchors.rdoc: 'Cognitive systems — attention, perception, working memory', description covering attention allocation, cognitive load, modality, processing fluency and scanning behaviour, cross_cutting: true }. The taxonomy schema gains an OPTIONAL cross_cutting boolean on a node (absent === false), and the inline taxonomySchema in tools/validate.ts widens from 6→7 nodes (minItems/maxItems 7, id pattern ^S[1-7]$) with the three size cross-checks updated to 7 and the failure message to S1–S7. lib/types.ts adds S7 to TaxonomyNodeId and cross_cutting?: boolean to TaxonomyNode (rule 13). Coverage is unaffected: computeRegistryTree() in lib/status.ts maps over loadTaxonomy().nodes, so S7 renders with zero children and an empty coverage line while the existing six nodes are byte-identical; the analyzer/gap-planner/packs never read the taxonomy. The /registry page shows a data-derived 'cross-cutting' tag beside the node name (not a status — the honesty rule holds) and the existing designed empty state for a node with no mechanisms. mechanism.schema.json parent pattern stays ^S[1-6]$ deliberately: this step only creates the node; nothing hangs off S7 yet, and widening the parent surface belongs to the step that admits S7 children. Verify: npm run validate green (taxonomy valid at 7 nodes, all cross-refs intact); npm run build green; Registry shows S7 with zero mechanisms and a cross-cutting marker; the six existing nodes' coverage is unchanged.",
      "area": "data"
    },
    {
      "id": "D-063",
      "date": "2026-07-20",
      "title": "S7 seeded with six candidates covering modality, load, multimedia design, fluency, scanning and foraging; all enter via the standard dossier gate",
      "body": "Six owner-provided candidate mechanisms enter under S7 (Perception & comprehension, D-062) as honest seed stubs in /registry/mechanisms/_seed/ — the same gate as every other mechanism, no shortcuts: they stay stubs until a dossier moves them through the lifecycle gate, and no content beyond what the owner provided was invented (rule 8). The roster: PS-13 Picture superiority / dual coding (grade_draft A), CL-14 Cognitive load (A), MM-15 Multimedia design principles (A), PF-16 Processing fluency (A-), SC-17 Scanning & visual attention (B), IF-18 Information foraging (B+). Each stub carries parent: S7, lifecycle_status: candidate, cross_cutting: true, plus owner-provided evidence_terms (5 each, including replication/boundary/critique angles per D-015) and pinned_evidence (D-017) — two pins each except IF-18 which has one (Pirolli & Card). SCHEMA SURFACE (the two changes D-062 deferred to 'the step that admits S7 children'): mechanism.schema.json $defs.seedStub widens parent from ^S[1-6]$ to ^S[1-7]$ and gains an OPTIONAL cross_cutting boolean (absent === false); the sub-schema stays additionalProperties:false and the stale '11 candidate seed stubs' wording is generalized. The FULL-record parent pattern stays ^S[1-6]$ deliberately — no S7 mechanism has passed the dossier gate yet, so widening the full-record surface belongs to the promotion step, not this one. lib/types.ts adds cross_cutting?: boolean to SeedStub, mirroring the sub-schema (rule 13). The registry showcase is unchanged code: computeRegistryTree() in lib/status.ts joins loadTaxonomy().nodes × loadSeedStubs(), so S7 now renders 6 candidates / 0 full records purely from the files (honesty rule holds; no status hardcoded). The _seed/README.md roster note is refreshed: the original SPEC.md §2 seeds were promoted to full records, and the folder now holds the S7 candidate roster. Verify: npm run validate green (six stubs valid against seedStub, all candidate, all parent S7, ids unique across the roster); npm run build green; /registry shows S7 with 6 candidates and 0 full records; no invented content beyond the owner-provided stubs.",
      "area": "data"
    },
    {
      "id": "D-064",
      "date": "2026-07-20",
      "title": "Full-record parent widened to S1–S7 and cross_cutting allowed on full records; the S7 promotion path is open",
      "body": "The two schema changes D-062/D-063 deferred to 'the step that admits S7 children' are made now, ahead of Step 4 (the six S7 candidates becoming full records or being rejected). mechanism.schema.json full-record parent pattern widens from ^S[1-6]$ to ^S[1-7]$, and an OPTIONAL cross_cutting boolean is added to the full-record properties (absent === false), mirroring the seedStub sub-schema (D-063) — without it, additionalProperties:false would reject a promoted S7 record that must carry cross_cutting:true. lib/types.ts adds cross_cutting?: boolean to Mechanism (rule 13). No other change: tools/validate.ts is schema-driven and its parent-in-taxonomy cross-check already passes for S7 (added by D-062); the /registry showcase is unchanged code that renders whatever files exist (honesty rule holds). This step opens the surface only — no S7 record is promoted here; authoring the six records + dossiers is Step 4 content, entered by the owner/Claude and committed verbatim (rule 8). Verify: npm run validate green (existing 12 full records + 6 stubs unaffected; schema still compiles); npm run build green.",
      "area": "data"
    },
    {
      "id": "D-065",
      "date": "2026-07-20",
      "title": "Corpus digest is the standard harvest → record-drafting hand-off, replacing raw corpus files in the drafting loop",
      "body": "A harvested corpus is 280–370 records of full abstracts — too large to read by hand when drafting a record + dossier. A new reader tool tools/corpus-digest.ts (npm run digest -- <MECHANISM-ID> [--top N] | --all) projects one corpus into the compact hand-off a drafter actually needs and writes it to /corpora/evidence/digests/{id}.md (markdown, also echoed to stdout). It is a READER, not a connector: it reads only local files (/corpora/evidence/{id}.json + the registry record or _seed stub for the pinned_evidence cross-check) and makes no network call — rule 12's whitelist is untouched, and it adds no science (rule 8), only re-projects what the harvest recorded. The digest carries: corpus stats (record count, fetched_at, terms + terms_source, query count), pins resolved/unresolved (declared pinned_evidence DOIs vs DOIs present in the corpus, missing DOIs listed), snowball_added + review-anchor status + coverage note, category_counts, and diversity (recency + novelty + viewpoint/source spread); then the top N records per evidence category (foundational / meta-analysis / replication / dissent / recent), ranked by citations, each with title, authors (first + et al.), year, venue, citations, DOI, source_api, cross-listed categories and search angles. Default N=15. lib/types.ts gains read-only mirror types for the corpus file (EvidenceCorpusFile and its parts — the writer's private interfaces live in tools/connectors/evidence.ts, lib never imports from tools/) plus the digest types (CorpusDigest, CorpusDigestEntry, CorpusDigestPins), no any (rule 13). package.json gains the digest script. THE POLICY: from this batch on, the digest is the standard harvest → drafting hand-off for every mechanism; the raw corpus stays on disk as provenance but is no longer read by hand while drafting. Digest .md files are generated output on the /corpora surface, not validated by tools/validate.ts (the render-cards precedent). Legacy pre-D-058 corpora carry no diversity_report, so the mirror type and the reader treat it as optional (novelty/recency shown as 'not recorded' rather than crashing). Verify: npm run digest -- PS-13 CL-14 MM-15 PF-16 SC-17 IF-18 writes six digests for the S7 candidates with pins resolved/unresolved reported and non-empty dissent lists; npm run digest -- --all additionally digests every core corpus with a registry entry; npm run validate and npm run build green.",
      "area": "operations"
    },
    {
      "id": "D-066",
      "date": "2026-07-20",
      "title": "Cross-cutting mechanisms are emitted into every pack automatically; pack-map stays motivational-only",
      "body": "Perception & comprehension (S7, cross_cutting: true, D-062) precedes every motivational mechanism, so its evidence must reach every generated element — but listing it per element in the pack map would duplicate the same knowledge across 11 packs and inflate the sufficiency matrix with cells that are identical everywhere. THE POLICY: cross-cutting mechanisms are emitted into every pack automatically; pack-map stays motivational-only. tools/render-packs.ts reads registry/taxonomy.json, collects the L0 ids flagged cross_cutting (today only S7), selects every FULL record whose parent is one of them (the renderer never reads _seed/, so the set is empty until the S7 candidates are promoted to full records in Step 4), sorts by id, and emits them into a distinct top-level section cross_cutting_perception in EVERY pack — same datasheet atom as LAYER 1 (id, name, fact, grade, source, boundary, active_when, realizations, forbidden via the shared buildMechanism), placed between LAYER 1 and LAYER 2, kept separate from the pack's own motivational mechanisms. lib/types.ts adds cross_cutting_perception: PackMechanism[] to PackDatasheet (rule 13). When the set is empty the section renders as an empty flow list with an honest comment pointing at docs/s7-drafting-brief.md (rule 15). LAYER 1 guard: any cross-cutting id that slips into an element's mechanism list is filtered out before LAYER 1 is built, so a perception mechanism is never duplicated as a motivational atom. LAYER 2: the authored-interaction lookup (D-057) widens its pair scope from the pack's members to members UNION the cross-cutting set, so an owner-authored interactions/{A}__{B}.json between a perception mechanism and a pack mechanism surfaces in LAYER 2; relation-derived (fallback) interactions stay members-only, matching 'where authored'. SCOPE: LAYER 3 context weights, HARD BOUNDARIES, and telemetry signals stay derived from the pack's own motivational mechanisms only — perception rows carry their forbidden ids inline but do not inflate every pack's boundary/signal sections. ENFORCEMENT: tools/validate.ts (pack-map section) now errors if any element's mechanisms list contains an id whose L0 parent is cross_cutting, so 'pack-map does NOT list S7 mechanisms per element' is a validated invariant, not a convention. Verify: npm run packs regenerates all 11 packs, each carrying the cross_cutting_perception section (empty list + comment for now); no pack duplicates a perception mechanism in LAYER 1; pack-paywall-conversion.yaml reproduces its reference structure with only the new section added; npm run validate and npm run build green.",
      "area": "architecture"
    },
    {
      "id": "D-067",
      "date": "2026-07-20",
      "title": "Perception scored as its own matrix row group; cross-cutting knowledge is not counted 11 times",
      "body": "Cross-cutting perception (S7, cross_cutting: true) is emitted into every pack by render-packs (D-066), but scoring its sufficiency per pack would multiply the SAME knowledge into all 11 pack rows — 165 identical-everywhere cells that would distort the overall-green figure up or down as a single roster changes. THE POLICY: the analyzer scores cross-cutting mechanisms ONCE per segment in their own matrix row group ('perception'), not multiplied into the pack rows; perception coverage is reported separately from pack coverage. tools/analyzer.ts reads registry/taxonomy.json, collects the L0 ids flagged cross_cutting (today only S7), selects every FULL record whose parent is one of them (never reads _seed/, so the roster is empty until Step 4 promotes the S7 candidates), and scores a single 'perception' row across the same active segments with the SAME five criteria, thresholds, and exhaustion as the packs. Its gaps are typed by the existing fix_type logic (harvest vs structural, D-055) unchanged. While the roster is empty the perception row is an honest all-red bootstrap row (every criterion 0, all five as typed gaps) rather than the vacuous greens an empty member list would score (interaction_coverage / freshness = 1). SufficiencyCell gains row_group?: 'pack' | 'perception' (lib/types.ts, rule 13; absent = legacy pack cell); the analyzer stamps every cell and bumps MATRIX_VERSION to 0.3.0. The 'perception' row id is reserved: tools/validate.ts errors if a pack-map element claims it, and the analyzer guards the same. Scoped runs (packs=a,b, the weekly loop) treat perception as a filterable pseudo-row — re-scorable via packs=perception, preserved verbatim otherwise; loadExistingCells fails loudly on a preserved cell lacking row_group, so one full npm run analyze upgrades the whole grid. COVERAGE: lib/maturation.ts computeCoverage partitions cells by row_group — overall / perPack / perSegment tally PACK cells only (denominator stays 11x15, so the overall-green figure neither jumps nor collapses), and a new perception StatusCounts reports the cross-cutting row apart. buildHeatmap bands rows into a packs group + a perception group (always one row, null cells = 'not analyzed' when the matrix predates the group); computeCellNovelty computes the perception row's low-novelty flag from the roster. app/maturation/page.tsx renders the perception row under a labelled group separator and a separate 'perception green' coverage line, never folded into the pack figure; no status literal is added (honesty rule, mapped through lib/status.ts). tools/gap-planner.ts registers the roster under the perception row id so a perception cell with a scored harvest gap expands to (mechanism x segment) harvest candidates once S7 has full records; structural gaps already route to authoring per cell. The weekly loop (.github/workflows/maturation.yml) treats a perception flip as a full re-render (its evidence lands in the cross_cutting_perception section of EVERY pack), not a scoped npm run packs. Verify: npm run analyze adds 15 perception cells (180 total) all red with all five typed gaps; the pack overall-green figure is unchanged vs the pre-change matrix and perception reports 0% separately; npm run gaps surfaces the perception cells in the authoring queue with no bogus harvest expansion; npm run validate and npm run build green; /maturation shows the perception row group red across 15 segments and the separate perception coverage line.",
      "area": "architecture"
    },
    {
      "id": "D-068",
      "date": "2026-07-20",
      "title": "Committed pack export bundle for team testing; generated by every pack render, surfaced read-only on the cockpit",
      "body": "Step 7 of the S7 phase calls for an export of the current pack set for team testing. Options considered: a download route in the app (new code surface, artifact not versioned), a GitHub link (no artifact at all), or a committed file. THE POLICY: tools/render-packs.ts writes /packs/export/packs-bundle.yaml — a multi-document YAML stream whose first document is a manifest (bundle: pack-export, version = pack-map version, pack_count, packs list, nature, source) followed by every pack-{id}.yaml verbatim (banners and comments preserved). The bundle is emitted by EVERY npm run packs run, full or scoped: it is always rebuilt from the pack files on disk (not just the packs rendered that run), so a scoped weekly-loop regenerate (D-052) still leaves a complete, current bundle. It carries NO timestamp — it is a pure function of the packs, so it only diffs when a pack diffs, and the team can read a week-over-week git diff of one file to see exactly what the guidance gained. Rationale for a committed file over an app route: the export stays versioned and diffable alongside the packs it came from, the app keeps zero new write/serve surface (constitution: file-based, narrow write surface D-023), and git is the handoff path the team already uses. Renderer self-check: the bundle must parse back as exactly pack_count + 1 YAML documents before writing. lib/types.ts gains PackBundleManifest (rule 13). ENFORCEMENT: tools/validate.ts pass 13b — when the bundle exists it must parse as multi-document YAML, open with the manifest, and the manifest's packs list / pack_count / document count must exactly match the pack-*.yaml files on disk (a mismatch means a stale or hand-edited bundle); an absent bundle is the honest pre-first-render state, not an error. COCKPIT: /maturation gains a 'Pack export — team testing' panel (lib/maturation.ts loadPackBundleManifest, fs-only) showing pack count, pack-map version, the bundled pack ids and the repo-relative handoff path, all read from the manifest at render time — nothing hardcoded (honesty rule); absent bundle renders a designed empty state (rule 15). The weekly loop needs no workflow change: its commit step already adds the packs/ tree. SCOPE NOTE: the Step 7 owner verify (perception sections present, evidence-toned, materially constraining density/modality/load) is deferred — the six S7 candidates are still _seed stubs with no harvested corpora, so cross_cutting_perception is honestly empty; when the records are promoted, npm run packs re-fills the sections and the bundle regenerates automatically, and the verify runs against that bundle diff. Verify now: npm run packs emits 11 packs + the bundle; a scoped run (packs=entry) leaves the bundle complete at 11; pack files byte-identical to the previous render; npm run validate and npm run build green.",
      "area": "architecture"
    },
    {
      "id": "D-069",
      "date": "2026-07-20",
      "title": "Interaction-authoring AUTHORED counter reads the /interactions store, not the (authored-pruned) authoring queue",
      "body": "The /maturation 'Interaction authoring' panel showed 'MISSING PAIRS N / AUTHORED 0' even after interactions/FR-11__SP-08.json was authored and committed, and the FR-11×SP-08 pair correctly no longer appeared in the missing-pairs list. TRACE (four questions): (1) the file exists at the exact path the readers use, /interactions/FR-11__SP-08.json, with the correct convention — double underscore, ids sorted by localeCompare (FR-11 < SP-08). No naming mismatch. (2) tools/analyzer.ts interaction_coverage DOES read the store, not just registry relations: it loads /interactions/*.json into authoredPairs and marks the pair covered, so the entry pack's affected cells score interaction_coverage 0.3333 (2/6) instead of 0.1667 (1/6) — the authored record is in the score. (3) ROOT CAUSE: the AUTHORED counter was derived from the authoring queue, not the interactions directory. lib/maturation.ts computeInteractionAuthoring builds its pair list ONLY from each task's missing_interaction_pairs, then computed authoredCount = pairs.filter(p => p.authored).length. But the analyzer DROPS an authored pair from missing_interaction_pairs the instant its record lands (D-057), so an authored pair never reappears in queue.tasks — a queue-derived authored tally is structurally pinned at ~0. (4) The missing-pairs list is correctly filtered to unauthored-only (the pair is absent because it is covered — correct behaviour, not a filter bug); only the AUTHORED display was wrong. THE FIX: authoredCount now reads authored.size — the count of records in the /interactions store (already loaded and passed in), which is what the panel hint 'records in /interactions/' has always promised. missingCount stays pairs.filter(p => !p.authored).length and each pair's authored flag/type is still read from the store, so even a stale queue that still listed an authored pair would render it 'authored' and exclude it from the missing tally (robust to queue staleness). No analyzer/gap-planner change: interaction_coverage already counts authored records and already prunes authored pairs from the queue. VALIDATOR: the naming/schema gate the issue asks for already exists — tools/validate.ts pass 14 (added with D-057) compiles interaction.schema.json, validates every /interactions/*.json, and cross-checks pair sorted + distinct, both ids in the registry roster, filename = {pair[0]}__{pair[1]}.json, and no pair authored twice; a single-vs-double-underscore filename, a reversed FR-11/SP-08 order, or a schema-invalid record fails CI rather than being silently skipped by the app reader (which catches-and-skips by design because the validator is the gate). lib/types.ts unchanged. Verify: /maturation now shows AUTHORED 1 with the FR-11×SP-08 record present, the missing-pairs list unchanged (the pair stays absent because it is covered), and interaction_coverage on the entry cells reflecting the record; npm run validate and npm run build green.",
      "area": "architecture"
    }
  ]
}
