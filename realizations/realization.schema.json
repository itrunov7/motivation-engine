{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ventora.dev/motivation-engine/realization.schema.json",
  "title": "Realization (L3 evidence, reported or inferred)",
  "description": "An interface, copy, or flow embodiment. derivation=reported describes what a source observed; derivation=inferred carries a product-UI pattern transferred from an effect measured in another domain, and must say so in domain_transfer (D-112). mechanism.implementations remains the product-authored directive layer.",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "mechanism_id": {
      "type": "string",
      "pattern": "^[A-Z]{2}-\\d{2}$"
    },
    "effect_refs": {
      "description": "First-class L2 effects this realization embodies. Named to match mechanism.effect_refs; it replaced the singular effect_id in D-112, when no record used that field yet.",
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" }
    },
    "derivation": {
      "description": "How this record came to exist. reported = a source described this embodiment. inferred = the pattern is a domain transfer from an effect nobody measured in a product context; it is a hypothesis for a generator, not evidence. Optional so records written before D-112 stay valid; required of anything the extraction pipeline authors, enforced in tools/validate.ts.",
      "enum": ["reported", "inferred"]
    },
    "domain_transfer": {
      "description": "The domain the evidence was measured in versus the domain the record is applied in. Equal domains mean no transfer. Carried explicitly rather than left implicit in effect.boundary, because the generator reads this record and never reads the effect (D-112).",
      "type": "object",
      "properties": {
        "source_domain": { "type": "string", "minLength": 1 },
        "application_domain": { "type": "string", "minLength": 1 }
      },
      "required": ["source_domain", "application_domain"],
      "additionalProperties": false
    },
    "term": { "type": "string", "minLength": 1 },
    "description_as_reported": {
      "description": "What the source states, in the source's own domain language. For an inferred record this is the EVIDENCE half and must not describe a product UI.",
      "type": "string",
      "minLength": 1
    },
    "pattern": {
      "description": "The implementable product-UI directive, present only when derivation=inferred. This is the INFERENCE half: concrete enough for a generator to act on, and separated from description_as_reported so a reader can see which half a source supports.",
      "type": "string",
      "minLength": 1
    },
    "parameters": {
      "description": "Every numeric threshold the pattern depends on, named and declared instead of stated as prose (D-115). A pattern that says a user must complete a task 'three times' asserts a measured quantity; no source measured it. The pattern text carries a {name} placeholder, the default lives here, and evidence_basis says what the number rests on — so a reader can see it is a tunable default and a generator can override it without editing prose.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/patternParameter" }
    },
    "artifact_context": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "provenance": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/provenanceItem" }
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    }
  },
  "required": [
    "id",
    "mechanism_id",
    "term",
    "description_as_reported",
    "artifact_context",
    "provenance",
    "confidence"
  ],
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": { "derivation": { "const": "inferred" } },
        "required": ["derivation"]
      },
      "then": {
        "properties": {
          "effect_refs": { "type": "array", "minItems": 1 },
          "domain_transfer": { "type": "object" },
          "pattern": { "type": "string", "minLength": 1 }
        },
        "required": ["effect_refs", "domain_transfer", "pattern"]
      }
    },
    {
      "if": {
        "properties": { "derivation": { "const": "reported" } },
        "required": ["derivation"]
      },
      "then": {
        "properties": {
          "domain_transfer": { "type": "object" },
          "pattern": { "type": "string" },
          "parameters": false
        },
        "required": ["domain_transfer"],
        "not": { "required": ["pattern"] }
      }
    }
  ],
  "$defs": {
    "doi": {
      "type": "string",
      "pattern": "^10\\.\\d{4,9}/\\S+$"
    },
    "patternParameter": {
      "description": "One tunable threshold referenced from `pattern` as {name}.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z0-9]+(?:_[a-z0-9]+)*$" },
        "value": { "type": "number" },
        "unit": {
          "description": "What the number counts, in words — the pattern text says {name}, so the unit is the only place a reader learns whether 3 means sessions, days, or completions.",
          "type": "string",
          "minLength": 1
        },
        "evidence_basis": {
          "description": "What the default rests on. One literal for now, following span_absent_reason in this same schema: a filterable value, not a sentence a writer can reword into sounding measured. New values are added here when a source actually measures a threshold.",
          "enum": ["none — default heuristic"]
        }
      },
      "required": ["name", "value", "unit", "evidence_basis"],
      "additionalProperties": false
    },
    "provenanceItem": {
      "oneOf": [
        { "$ref": "#/$defs/evidenceProvenanceItem" },
        { "$ref": "#/$defs/realizationProvenanceItem" },
        { "$ref": "#/$defs/inferenceProvenanceItem" }
      ]
    },
    "evidenceProvenanceItem": {
      "type": "object",
      "properties": {
        "corpus_kind": { "const": "evidence" },
        "mechanism_id": { "type": "string", "pattern": "^[A-Z]{2}-\\d{2}$" },
        "corpus_record_id": { "type": "string", "pattern": "^cr_[a-f0-9]{24}$" },
        "doi": {
          "anyOf": [
            { "$ref": "#/$defs/doi" },
            { "type": "null" }
          ]
        },
        "title": { "type": "string", "minLength": 1 },
        "quote_or_locus": { "type": "string", "minLength": 1 },
        "source_span": { "$ref": "#/$defs/provenanceSourceSpan" }
      },
      "required": ["mechanism_id", "corpus_record_id", "doi", "title", "quote_or_locus"],
      "additionalProperties": false
    },
    "provenanceSourceSpan": {
      "description": "Where the quote sits in its source record, and which version of that record the offsets were resolved against (D-110). Carried on EVIDENCE provenance only: realizationProvenanceItem and inferenceProvenanceItem deliberately have no span, and each states why, so the gap is declared rather than assumed. Re-slicing lib/proposal-quality evidenceSourceText(record) at start/end must reproduce quote_or_locus byte for byte; a source_text_sha256 mismatch marks the span STALE.",
      "type": "object",
      "properties": {
        "start": { "type": "integer", "minimum": 0 },
        "end": { "type": "integer", "minimum": 1 },
        "source_text_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
      },
      "required": ["start", "end", "source_text_sha256"],
      "additionalProperties": false
    },
    "realizationProvenanceItem": {
      "type": "object",
      "properties": {
        "corpus_kind": { "const": "realization" },
        "mechanism_id": { "type": "string", "pattern": "^[A-Z]{2}-\\d{2}$" },
        "corpus_record_id": { "type": "string", "pattern": "^rr_[a-f0-9]{24}$" },
        "source_id": { "type": "string", "minLength": 1 },
        "title": { "type": "string", "minLength": 1 },
        "quote_or_locus": { "type": "string", "minLength": 1 },
        "contributed_by": { "type": ["string", "null"], "minLength": 1 }
      },
      "required": [
        "corpus_kind",
        "mechanism_id",
        "corpus_record_id",
        "source_id",
        "title",
        "quote_or_locus",
        "contributed_by"
      ],
      "additionalProperties": false
    },
    "inferenceProvenanceItem": {
      "description": "The transfer step itself, recorded as provenance (D-112). quote_or_locus is the effect's own statement, copied verbatim; there is no source_span because the statement is an L2 record's text, not a slice of a harvested document. span_absent_reason states that in the data instead of leaving an empty field to be read as an oversight. corpus_record_id and title name the evidence record the EFFECT cites, so the trail stays walkable to a source the owner can open.",
      "type": "object",
      "properties": {
        "corpus_kind": { "const": "inference" },
        "mechanism_id": { "type": "string", "pattern": "^[A-Z]{2}-\\d{2}$" },
        "corpus_record_id": { "type": "string", "pattern": "^cr_[a-f0-9]{24}$" },
        "effect_id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
        "title": { "type": "string", "minLength": 1 },
        "quote_or_locus": { "type": "string", "minLength": 1 },
        "span_absent_reason": { "const": "no direct span — inferred from effect" }
      },
      "required": [
        "corpus_kind",
        "mechanism_id",
        "corpus_record_id",
        "effect_id",
        "title",
        "quote_or_locus",
        "span_absent_reason"
      ],
      "additionalProperties": false
    }
  }
}
