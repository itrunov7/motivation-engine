# Auth (SPEC §5). Copy to .env.local and fill in.
# ACCESS_PASSWORD — the single shared password for the Control Center.
# SESSION_SECRET — random 32-byte hex for HMAC cookie signing: `openssl rand -hex 32`.
ACCESS_PASSWORD=
SESSION_SECRET=
